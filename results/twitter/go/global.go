// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"sync"
	"github.com/boltdb/bolt"
)

func mustJSON(value any) []byte {
	bytes, err := json.Marshal(value)
	if err != nil {
		panic(err)
	}
	return bytes
}

// Global variable for the single-row write-back cache with a RWMutex.
var userCache struct {
	lock    sync.RWMutex
	row     *User
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var followersCache struct {
	lock    sync.RWMutex
	row     *Followers
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var followsCache struct {
	lock    sync.RWMutex
	row     *Follows
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var tweetsCache struct {
	lock    sync.RWMutex
	row     *Tweets
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var added_tweetsCache struct {
	lock    sync.RWMutex
	row     *AddedTweets
	isDirty bool
}

// FlushAllCaches flushes all table caches
func FlushAllCaches(tx *bolt.Tx) {
	flushUserCache(tx)
	flushFollowersCache(tx)
	flushFollowsCache(tx)
	flushTweetsCache(tx)
	flushAddedTweetsCache(tx)
}

// Iterator getter functions for table primary keys
// Generated for each primary key field in each table

// get_uid_21 extracts uid from the iterator for table User
func get_uid_21(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uid_21: iterator is not valid")
	}

	// Deserialize the composite key to extract uid
	type CompositeKey struct {
		uid uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uid
}

// get_f1_22 extracts f1 from the iterator for table Followers
func get_f1_22(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_f1_22: iterator is not valid")
	}

	// Deserialize the composite key to extract f1
	type CompositeKey struct {
		f1 uint64
		f2 uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.f1
}

// get_f2_23 extracts f2 from the iterator for table Followers
func get_f2_23(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_f2_23: iterator is not valid")
	}

	// Deserialize the composite key to extract f2
	type CompositeKey struct {
		f1 uint64
		f2 uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.f2
}

// get_f1_24 extracts f1 from the iterator for table Follows
func get_f1_24(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_f1_24: iterator is not valid")
	}

	// Deserialize the composite key to extract f1
	type CompositeKey struct {
		f1 uint64
		f2 uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.f1
}

// get_f2_25 extracts f2 from the iterator for table Follows
func get_f2_25(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_f2_25: iterator is not valid")
	}

	// Deserialize the composite key to extract f2
	type CompositeKey struct {
		f1 uint64
		f2 uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.f2
}

// get_uid_26 extracts uid from the iterator for table Tweets
func get_uid_26(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uid_26: iterator is not valid")
	}

	// Deserialize the composite key to extract uid
	type CompositeKey struct {
		uid uint64
		id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uid
}

// get_id_27 extracts id from the iterator for table Tweets
func get_id_27(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_id_27: iterator is not valid")
	}

	// Deserialize the composite key to extract id
	type CompositeKey struct {
		uid uint64
		id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.id
}

// get_uid_28 extracts uid from the iterator for table AddedTweets
func get_uid_28(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uid_28: iterator is not valid")
	}

	// Deserialize the composite key to extract uid
	type CompositeKey struct {
		uid uint64
		id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uid
}

// get_id_29 extracts id from the iterator for table AddedTweets
func get_id_29(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_id_29: iterator is not valid")
	}

	// Deserialize the composite key to extract id
	type CompositeKey struct {
		uid uint64
		id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.id
}

const (
	__slice__ uint64 = 0
	__shards__ uint64 = 1
)

type UserKey struct {
	uid uint64
}

type User struct {
	Key UserKey
	name string
}

// flushUserCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushUserCacheUnlocked(tx *bolt.Tx) {
	if userCache.row != nil && userCache.isDirty {
		bucket := tx.Bucket([]byte("tUser"))
		if bucket == nil {
			panic("missing bucket tUser")
		}
		var key UserKey = userCache.row.Key
		var row User = *userCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	userCache.row = nil
	userCache.isDirty = false
}

// flushUserCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushUserCache(tx *bolt.Tx) {
	userCache.lock.Lock()
	defer userCache.lock.Unlock()
	flushUserCacheUnlocked(tx)
}

// getUser fetches data, utilizing the cache in a highly concurrent and safe manner.
func getUser(tx *bolt.Tx, key UserKey) ([]byte, User) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	userCache.lock.RLock()
	if userCache.row != nil && userCache.row.Key == key {
		row := *userCache.row
		userCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	userCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	userCache.lock.Lock()
	defer userCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if userCache.row != nil && userCache.row.Key == key {
		return mustJSON(key), *userCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushUserCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tUser"))
	if bucket == nil {
		panic("missing bucket tUser")
	}
	keyBytes := mustJSON(key)
	var row User
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	userCache.row = &row
	userCache.isDirty = false

	return keyBytes, row
}

// putUser updates the cache and marks it as dirty (write-back).
func putUser(tx *bolt.Tx, key UserKey, row User) {
	userCache.lock.Lock()
	defer userCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if userCache.row != nil && userCache.row.Key != key {
		flushUserCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	userCache.row = &row
	userCache.isDirty = true
}

func getUserPar(key UserKey) uint64 {
	// Call partition function to determine shard
	return p(key.uid)
}

func putUserPar(key UserKey) uint64 {
	// Call partition function to determine shard
	return p(key.uid)
}

type FollowersKey struct {
	f1 uint64
	f2 uint64
}

type Followers struct {
	Key FollowersKey
}

// flushFollowersCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushFollowersCacheUnlocked(tx *bolt.Tx) {
	if followersCache.row != nil && followersCache.isDirty {
		bucket := tx.Bucket([]byte("tFollowers"))
		if bucket == nil {
			panic("missing bucket tFollowers")
		}
		var key FollowersKey = followersCache.row.Key
		var row Followers = *followersCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	followersCache.row = nil
	followersCache.isDirty = false
}

// flushFollowersCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushFollowersCache(tx *bolt.Tx) {
	followersCache.lock.Lock()
	defer followersCache.lock.Unlock()
	flushFollowersCacheUnlocked(tx)
}

// getFollowers fetches data, utilizing the cache in a highly concurrent and safe manner.
func getFollowers(tx *bolt.Tx, key FollowersKey) ([]byte, Followers) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	followersCache.lock.RLock()
	if followersCache.row != nil && followersCache.row.Key == key {
		row := *followersCache.row
		followersCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	followersCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	followersCache.lock.Lock()
	defer followersCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if followersCache.row != nil && followersCache.row.Key == key {
		return mustJSON(key), *followersCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushFollowersCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tFollowers"))
	if bucket == nil {
		panic("missing bucket tFollowers")
	}
	keyBytes := mustJSON(key)
	var row Followers
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	followersCache.row = &row
	followersCache.isDirty = false

	return keyBytes, row
}

// putFollowers updates the cache and marks it as dirty (write-back).
func putFollowers(tx *bolt.Tx, key FollowersKey, row Followers) {
	followersCache.lock.Lock()
	defer followersCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if followersCache.row != nil && followersCache.row.Key != key {
		flushFollowersCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	followersCache.row = &row
	followersCache.isDirty = true
}

func getFollowersPar(key FollowersKey) uint64 {
	// Call partition function to determine shard
	return p(key.f1)
}

func putFollowersPar(key FollowersKey) uint64 {
	// Call partition function to determine shard
	return p(key.f1)
}

type FollowsKey struct {
	f1 uint64
	f2 uint64
}

type Follows struct {
	Key FollowsKey
}

// flushFollowsCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushFollowsCacheUnlocked(tx *bolt.Tx) {
	if followsCache.row != nil && followsCache.isDirty {
		bucket := tx.Bucket([]byte("tFollows"))
		if bucket == nil {
			panic("missing bucket tFollows")
		}
		var key FollowsKey = followsCache.row.Key
		var row Follows = *followsCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	followsCache.row = nil
	followsCache.isDirty = false
}

// flushFollowsCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushFollowsCache(tx *bolt.Tx) {
	followsCache.lock.Lock()
	defer followsCache.lock.Unlock()
	flushFollowsCacheUnlocked(tx)
}

// getFollows fetches data, utilizing the cache in a highly concurrent and safe manner.
func getFollows(tx *bolt.Tx, key FollowsKey) ([]byte, Follows) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	followsCache.lock.RLock()
	if followsCache.row != nil && followsCache.row.Key == key {
		row := *followsCache.row
		followsCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	followsCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	followsCache.lock.Lock()
	defer followsCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if followsCache.row != nil && followsCache.row.Key == key {
		return mustJSON(key), *followsCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushFollowsCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tFollows"))
	if bucket == nil {
		panic("missing bucket tFollows")
	}
	keyBytes := mustJSON(key)
	var row Follows
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	followsCache.row = &row
	followsCache.isDirty = false

	return keyBytes, row
}

// putFollows updates the cache and marks it as dirty (write-back).
func putFollows(tx *bolt.Tx, key FollowsKey, row Follows) {
	followsCache.lock.Lock()
	defer followsCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if followsCache.row != nil && followsCache.row.Key != key {
		flushFollowsCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	followsCache.row = &row
	followsCache.isDirty = true
}

func getFollowsPar(key FollowsKey) uint64 {
	// Call partition function to determine shard
	return p(key.f1)
}

func putFollowsPar(key FollowsKey) uint64 {
	// Call partition function to determine shard
	return p(key.f1)
}

type TweetsKey struct {
	uid uint64
	id uint64
}

type Tweets struct {
	Key TweetsKey
	text string
	createdate uint64
}

// flushTweetsCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushTweetsCacheUnlocked(tx *bolt.Tx) {
	if tweetsCache.row != nil && tweetsCache.isDirty {
		bucket := tx.Bucket([]byte("tTweets"))
		if bucket == nil {
			panic("missing bucket tTweets")
		}
		var key TweetsKey = tweetsCache.row.Key
		var row Tweets = *tweetsCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	tweetsCache.row = nil
	tweetsCache.isDirty = false
}

// flushTweetsCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushTweetsCache(tx *bolt.Tx) {
	tweetsCache.lock.Lock()
	defer tweetsCache.lock.Unlock()
	flushTweetsCacheUnlocked(tx)
}

// getTweets fetches data, utilizing the cache in a highly concurrent and safe manner.
func getTweets(tx *bolt.Tx, key TweetsKey) ([]byte, Tweets) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	tweetsCache.lock.RLock()
	if tweetsCache.row != nil && tweetsCache.row.Key == key {
		row := *tweetsCache.row
		tweetsCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	tweetsCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	tweetsCache.lock.Lock()
	defer tweetsCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if tweetsCache.row != nil && tweetsCache.row.Key == key {
		return mustJSON(key), *tweetsCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushTweetsCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tTweets"))
	if bucket == nil {
		panic("missing bucket tTweets")
	}
	keyBytes := mustJSON(key)
	var row Tweets
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	tweetsCache.row = &row
	tweetsCache.isDirty = false

	return keyBytes, row
}

// putTweets updates the cache and marks it as dirty (write-back).
func putTweets(tx *bolt.Tx, key TweetsKey, row Tweets) {
	tweetsCache.lock.Lock()
	defer tweetsCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if tweetsCache.row != nil && tweetsCache.row.Key != key {
		flushTweetsCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	tweetsCache.row = &row
	tweetsCache.isDirty = true
}

func getTweetsPar(key TweetsKey) uint64 {
	// Call partition function to determine shard
	return p(key.uid)
}

func putTweetsPar(key TweetsKey) uint64 {
	// Call partition function to determine shard
	return p(key.uid)
}

type AddedTweetsKey struct {
	uid uint64
	id uint64
}

type AddedTweets struct {
	Key AddedTweetsKey
	text string
	createdate uint64
}

// flushAddedTweetsCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushAddedTweetsCacheUnlocked(tx *bolt.Tx) {
	if added_tweetsCache.row != nil && added_tweetsCache.isDirty {
		bucket := tx.Bucket([]byte("tAddedTweets"))
		if bucket == nil {
			panic("missing bucket tAddedTweets")
		}
		var key AddedTweetsKey = added_tweetsCache.row.Key
		var row AddedTweets = *added_tweetsCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	added_tweetsCache.row = nil
	added_tweetsCache.isDirty = false
}

// flushAddedTweetsCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushAddedTweetsCache(tx *bolt.Tx) {
	added_tweetsCache.lock.Lock()
	defer added_tweetsCache.lock.Unlock()
	flushAddedTweetsCacheUnlocked(tx)
}

// getAddedTweets fetches data, utilizing the cache in a highly concurrent and safe manner.
func getAddedTweets(tx *bolt.Tx, key AddedTweetsKey) ([]byte, AddedTweets) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	added_tweetsCache.lock.RLock()
	if added_tweetsCache.row != nil && added_tweetsCache.row.Key == key {
		row := *added_tweetsCache.row
		added_tweetsCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	added_tweetsCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	added_tweetsCache.lock.Lock()
	defer added_tweetsCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if added_tweetsCache.row != nil && added_tweetsCache.row.Key == key {
		return mustJSON(key), *added_tweetsCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushAddedTweetsCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tAddedTweets"))
	if bucket == nil {
		panic("missing bucket tAddedTweets")
	}
	keyBytes := mustJSON(key)
	var row AddedTweets
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	added_tweetsCache.row = &row
	added_tweetsCache.isDirty = false

	return keyBytes, row
}

// putAddedTweets updates the cache and marks it as dirty (write-back).
func putAddedTweets(tx *bolt.Tx, key AddedTweetsKey, row AddedTweets) {
	added_tweetsCache.lock.Lock()
	defer added_tweetsCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if added_tweetsCache.row != nil && added_tweetsCache.row.Key != key {
		flushAddedTweetsCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	added_tweetsCache.row = &row
	added_tweetsCache.isDirty = true
}

func getAddedTweetsPar(key AddedTweetsKey) uint64 {
	// Call partition function to determine shard
	return p(key.uid)
}

func putAddedTweetsPar(key AddedTweetsKey) uint64 {
	// Call partition function to determine shard
	return p(key.uid)
}

func p(x uint64) uint64 {
	goto BB0

BB0:
	return x
}

func Scheds() {
	// Initialize scheduler maps
	LockSchedulers = make(map[string]*Scheduler)
	AccessSchedulers = make(map[string]*Scheduler)
	AccessDSchedulers = make(map[string]*Scheduler)

	// Create one lock scheduler for each table
	LockSchedulers["User"] = NewScheduler()
	LockSchedulers["Followers"] = NewScheduler()
	LockSchedulers["Follows"] = NewScheduler()
	LockSchedulers["Tweets"] = NewScheduler()
	LockSchedulers["AddedTweets"] = NewScheduler()

}

func Chains(db *bolt.DB) []*Chain {
	return []*Chain{
		GetfollowersChainImpl(db),
		GettweetChainImpl(db),
		GettweetsfromfollowingChainImpl(db),
		GetusertweetsChainImpl(db),
		InserttweetChainImpl(db),
	}
}

