// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"sync"
	"github.com/boltdb/bolt"
)

func mustJSON(value any) []byte {
	bytes, err := json.Marshal(value)
	if err != nil {
		panic(err)
	}
	return bytes
}

// Global variable for the single-row write-back cache with a RWMutex.
var date_dimCache struct {
	lock    sync.RWMutex
	row     *DateDim
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var time_dimCache struct {
	lock    sync.RWMutex
	row     *TimeDim
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var promotionCache struct {
	lock    sync.RWMutex
	row     *Promotion
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var itemCache struct {
	lock    sync.RWMutex
	row     *Item
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var customerCache struct {
	lock    sync.RWMutex
	row     *Customer
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var customer_demographicsCache struct {
	lock    sync.RWMutex
	row     *CustomerDemographics
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var customer_addressCache struct {
	lock    sync.RWMutex
	row     *CustomerAddress
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var storeCache struct {
	lock    sync.RWMutex
	row     *Store
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var warehouseCache struct {
	lock    sync.RWMutex
	row     *Warehouse
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var store_salesCache struct {
	lock    sync.RWMutex
	row     *StoreSales
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var store_returnsCache struct {
	lock    sync.RWMutex
	row     *StoreReturns
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var catalog_salesCache struct {
	lock    sync.RWMutex
	row     *CatalogSales
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var catalog_returnsCache struct {
	lock    sync.RWMutex
	row     *CatalogReturns
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var web_salesCache struct {
	lock    sync.RWMutex
	row     *WebSales
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var web_returnsCache struct {
	lock    sync.RWMutex
	row     *WebReturns
	isDirty bool
}

// FlushAllCaches flushes all table caches
func FlushAllCaches(tx *bolt.Tx) {
	flushDateDimCache(tx)
	flushTimeDimCache(tx)
	flushPromotionCache(tx)
	flushItemCache(tx)
	flushCustomerCache(tx)
	flushCustomerDemographicsCache(tx)
	flushCustomerAddressCache(tx)
	flushStoreCache(tx)
	flushWarehouseCache(tx)
	flushStoreSalesCache(tx)
	flushStoreReturnsCache(tx)
	flushCatalogSalesCache(tx)
	flushCatalogReturnsCache(tx)
	flushWebSalesCache(tx)
	flushWebReturnsCache(tx)
}

// Iterator getter functions for table primary keys
// Generated for each primary key field in each table

// get_d_date_sk_26 extracts d_date_sk from the iterator for table DateDim
func get_d_date_sk_26(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_d_date_sk_26: iterator is not valid")
	}

	// Deserialize the composite key to extract d_date_sk
	type CompositeKey struct {
		d_date_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.d_date_sk
}

// get_t_time_sk_27 extracts t_time_sk from the iterator for table TimeDim
func get_t_time_sk_27(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_t_time_sk_27: iterator is not valid")
	}

	// Deserialize the composite key to extract t_time_sk
	type CompositeKey struct {
		t_time_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.t_time_sk
}

// get_p_promo_sk_28 extracts p_promo_sk from the iterator for table Promotion
func get_p_promo_sk_28(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_p_promo_sk_28: iterator is not valid")
	}

	// Deserialize the composite key to extract p_promo_sk
	type CompositeKey struct {
		p_promo_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.p_promo_sk
}

// get_i_item_sk_29 extracts i_item_sk from the iterator for table Item
func get_i_item_sk_29(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_i_item_sk_29: iterator is not valid")
	}

	// Deserialize the composite key to extract i_item_sk
	type CompositeKey struct {
		i_item_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.i_item_sk
}

// get_c_customer_sk_30 extracts c_customer_sk from the iterator for table Customer
func get_c_customer_sk_30(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_c_customer_sk_30: iterator is not valid")
	}

	// Deserialize the composite key to extract c_customer_sk
	type CompositeKey struct {
		c_customer_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.c_customer_sk
}

// get_cd_demo_sk_31 extracts cd_demo_sk from the iterator for table CustomerDemographics
func get_cd_demo_sk_31(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_cd_demo_sk_31: iterator is not valid")
	}

	// Deserialize the composite key to extract cd_demo_sk
	type CompositeKey struct {
		cd_demo_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.cd_demo_sk
}

// get_ca_address_sk_32 extracts ca_address_sk from the iterator for table CustomerAddress
func get_ca_address_sk_32(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ca_address_sk_32: iterator is not valid")
	}

	// Deserialize the composite key to extract ca_address_sk
	type CompositeKey struct {
		ca_address_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ca_address_sk
}

// get_s_store_sk_33 extracts s_store_sk from the iterator for table Store
func get_s_store_sk_33(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_s_store_sk_33: iterator is not valid")
	}

	// Deserialize the composite key to extract s_store_sk
	type CompositeKey struct {
		s_store_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.s_store_sk
}

// get_w_warehouse_sk_34 extracts w_warehouse_sk from the iterator for table Warehouse
func get_w_warehouse_sk_34(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_w_warehouse_sk_34: iterator is not valid")
	}

	// Deserialize the composite key to extract w_warehouse_sk
	type CompositeKey struct {
		w_warehouse_sk uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.w_warehouse_sk
}

// get_ss_item_sk_35 extracts ss_item_sk from the iterator for table StoreSales
func get_ss_item_sk_35(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ss_item_sk_35: iterator is not valid")
	}

	// Deserialize the composite key to extract ss_item_sk
	type CompositeKey struct {
		ss_item_sk uint64
		ss_ticket_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ss_item_sk
}

// get_ss_ticket_number_36 extracts ss_ticket_number from the iterator for table StoreSales
func get_ss_ticket_number_36(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ss_ticket_number_36: iterator is not valid")
	}

	// Deserialize the composite key to extract ss_ticket_number
	type CompositeKey struct {
		ss_item_sk uint64
		ss_ticket_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ss_ticket_number
}

// get_sr_item_sk_37 extracts sr_item_sk from the iterator for table StoreReturns
func get_sr_item_sk_37(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_sr_item_sk_37: iterator is not valid")
	}

	// Deserialize the composite key to extract sr_item_sk
	type CompositeKey struct {
		sr_item_sk uint64
		sr_ticket_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.sr_item_sk
}

// get_sr_ticket_number_38 extracts sr_ticket_number from the iterator for table StoreReturns
func get_sr_ticket_number_38(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_sr_ticket_number_38: iterator is not valid")
	}

	// Deserialize the composite key to extract sr_ticket_number
	type CompositeKey struct {
		sr_item_sk uint64
		sr_ticket_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.sr_ticket_number
}

// get_cs_item_sk_39 extracts cs_item_sk from the iterator for table CatalogSales
func get_cs_item_sk_39(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_cs_item_sk_39: iterator is not valid")
	}

	// Deserialize the composite key to extract cs_item_sk
	type CompositeKey struct {
		cs_item_sk uint64
		cs_order_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.cs_item_sk
}

// get_cs_order_number_40 extracts cs_order_number from the iterator for table CatalogSales
func get_cs_order_number_40(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_cs_order_number_40: iterator is not valid")
	}

	// Deserialize the composite key to extract cs_order_number
	type CompositeKey struct {
		cs_item_sk uint64
		cs_order_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.cs_order_number
}

// get_cr_item_sk_41 extracts cr_item_sk from the iterator for table CatalogReturns
func get_cr_item_sk_41(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_cr_item_sk_41: iterator is not valid")
	}

	// Deserialize the composite key to extract cr_item_sk
	type CompositeKey struct {
		cr_item_sk uint64
		cr_order_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.cr_item_sk
}

// get_cr_order_number_42 extracts cr_order_number from the iterator for table CatalogReturns
func get_cr_order_number_42(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_cr_order_number_42: iterator is not valid")
	}

	// Deserialize the composite key to extract cr_order_number
	type CompositeKey struct {
		cr_item_sk uint64
		cr_order_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.cr_order_number
}

// get_ws_item_sk_43 extracts ws_item_sk from the iterator for table WebSales
func get_ws_item_sk_43(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ws_item_sk_43: iterator is not valid")
	}

	// Deserialize the composite key to extract ws_item_sk
	type CompositeKey struct {
		ws_item_sk uint64
		ws_order_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ws_item_sk
}

// get_ws_order_number_44 extracts ws_order_number from the iterator for table WebSales
func get_ws_order_number_44(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ws_order_number_44: iterator is not valid")
	}

	// Deserialize the composite key to extract ws_order_number
	type CompositeKey struct {
		ws_item_sk uint64
		ws_order_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ws_order_number
}

// get_wr_item_sk_45 extracts wr_item_sk from the iterator for table WebReturns
func get_wr_item_sk_45(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_wr_item_sk_45: iterator is not valid")
	}

	// Deserialize the composite key to extract wr_item_sk
	type CompositeKey struct {
		wr_item_sk uint64
		wr_order_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.wr_item_sk
}

// get_wr_order_number_46 extracts wr_order_number from the iterator for table WebReturns
func get_wr_order_number_46(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_wr_order_number_46: iterator is not valid")
	}

	// Deserialize the composite key to extract wr_order_number
	type CompositeKey struct {
		wr_item_sk uint64
		wr_order_number uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.wr_order_number
}

const (
	__slice__ uint64 = 0
	__shards__ uint64 = 1
)

type DateDimKey struct {
	d_date_sk uint64
}

type DateDim struct {
	Key DateDimKey
	d_date uint64
	d_year uint64
	d_month_seq uint64
	d_qoy uint64
	d_moy uint64
	d_day_name string
}

// flushDateDimCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushDateDimCacheUnlocked(tx *bolt.Tx) {
	if date_dimCache.row != nil && date_dimCache.isDirty {
		bucket := tx.Bucket([]byte("tDateDim"))
		if bucket == nil {
			panic("missing bucket tDateDim")
		}
		var key DateDimKey = date_dimCache.row.Key
		var row DateDim = *date_dimCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	date_dimCache.row = nil
	date_dimCache.isDirty = false
}

// flushDateDimCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushDateDimCache(tx *bolt.Tx) {
	date_dimCache.lock.Lock()
	defer date_dimCache.lock.Unlock()
	flushDateDimCacheUnlocked(tx)
}

// getDateDim fetches data, utilizing the cache in a highly concurrent and safe manner.
func getDateDim(tx *bolt.Tx, key DateDimKey) ([]byte, DateDim) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	date_dimCache.lock.RLock()
	if date_dimCache.row != nil && date_dimCache.row.Key == key {
		row := *date_dimCache.row
		date_dimCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	date_dimCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	date_dimCache.lock.Lock()
	defer date_dimCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if date_dimCache.row != nil && date_dimCache.row.Key == key {
		return mustJSON(key), *date_dimCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushDateDimCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tDateDim"))
	if bucket == nil {
		panic("missing bucket tDateDim")
	}
	keyBytes := mustJSON(key)
	var row DateDim
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	date_dimCache.row = &row
	date_dimCache.isDirty = false

	return keyBytes, row
}

// putDateDim updates the cache and marks it as dirty (write-back).
func putDateDim(tx *bolt.Tx, key DateDimKey, row DateDim) {
	date_dimCache.lock.Lock()
	defer date_dimCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if date_dimCache.row != nil && date_dimCache.row.Key != key {
		flushDateDimCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	date_dimCache.row = &row
	date_dimCache.isDirty = true
}

func getDateDimPar(key DateDimKey) uint64 {
	// Call partition function to determine shard
	return g(key.d_date_sk)
}

func putDateDimPar(key DateDimKey) uint64 {
	// Call partition function to determine shard
	return g(key.d_date_sk)
}

type TimeDimKey struct {
	t_time_sk uint64
}

type TimeDim struct {
	Key TimeDimKey
	t_hour uint64
	t_minute uint64
	t_am_pm string
}

// flushTimeDimCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushTimeDimCacheUnlocked(tx *bolt.Tx) {
	if time_dimCache.row != nil && time_dimCache.isDirty {
		bucket := tx.Bucket([]byte("tTimeDim"))
		if bucket == nil {
			panic("missing bucket tTimeDim")
		}
		var key TimeDimKey = time_dimCache.row.Key
		var row TimeDim = *time_dimCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	time_dimCache.row = nil
	time_dimCache.isDirty = false
}

// flushTimeDimCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushTimeDimCache(tx *bolt.Tx) {
	time_dimCache.lock.Lock()
	defer time_dimCache.lock.Unlock()
	flushTimeDimCacheUnlocked(tx)
}

// getTimeDim fetches data, utilizing the cache in a highly concurrent and safe manner.
func getTimeDim(tx *bolt.Tx, key TimeDimKey) ([]byte, TimeDim) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	time_dimCache.lock.RLock()
	if time_dimCache.row != nil && time_dimCache.row.Key == key {
		row := *time_dimCache.row
		time_dimCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	time_dimCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	time_dimCache.lock.Lock()
	defer time_dimCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if time_dimCache.row != nil && time_dimCache.row.Key == key {
		return mustJSON(key), *time_dimCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushTimeDimCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tTimeDim"))
	if bucket == nil {
		panic("missing bucket tTimeDim")
	}
	keyBytes := mustJSON(key)
	var row TimeDim
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	time_dimCache.row = &row
	time_dimCache.isDirty = false

	return keyBytes, row
}

// putTimeDim updates the cache and marks it as dirty (write-back).
func putTimeDim(tx *bolt.Tx, key TimeDimKey, row TimeDim) {
	time_dimCache.lock.Lock()
	defer time_dimCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if time_dimCache.row != nil && time_dimCache.row.Key != key {
		flushTimeDimCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	time_dimCache.row = &row
	time_dimCache.isDirty = true
}

func getTimeDimPar(key TimeDimKey) uint64 {
	// Call partition function to determine shard
	return g(key.t_time_sk)
}

func putTimeDimPar(key TimeDimKey) uint64 {
	// Call partition function to determine shard
	return g(key.t_time_sk)
}

type PromotionKey struct {
	p_promo_sk uint64
}

type Promotion struct {
	Key PromotionKey
	p_promo_name string
	p_channel_email string
	p_channel_tv string
	p_channel_radio string
}

// flushPromotionCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushPromotionCacheUnlocked(tx *bolt.Tx) {
	if promotionCache.row != nil && promotionCache.isDirty {
		bucket := tx.Bucket([]byte("tPromotion"))
		if bucket == nil {
			panic("missing bucket tPromotion")
		}
		var key PromotionKey = promotionCache.row.Key
		var row Promotion = *promotionCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	promotionCache.row = nil
	promotionCache.isDirty = false
}

// flushPromotionCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushPromotionCache(tx *bolt.Tx) {
	promotionCache.lock.Lock()
	defer promotionCache.lock.Unlock()
	flushPromotionCacheUnlocked(tx)
}

// getPromotion fetches data, utilizing the cache in a highly concurrent and safe manner.
func getPromotion(tx *bolt.Tx, key PromotionKey) ([]byte, Promotion) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	promotionCache.lock.RLock()
	if promotionCache.row != nil && promotionCache.row.Key == key {
		row := *promotionCache.row
		promotionCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	promotionCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	promotionCache.lock.Lock()
	defer promotionCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if promotionCache.row != nil && promotionCache.row.Key == key {
		return mustJSON(key), *promotionCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushPromotionCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tPromotion"))
	if bucket == nil {
		panic("missing bucket tPromotion")
	}
	keyBytes := mustJSON(key)
	var row Promotion
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	promotionCache.row = &row
	promotionCache.isDirty = false

	return keyBytes, row
}

// putPromotion updates the cache and marks it as dirty (write-back).
func putPromotion(tx *bolt.Tx, key PromotionKey, row Promotion) {
	promotionCache.lock.Lock()
	defer promotionCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if promotionCache.row != nil && promotionCache.row.Key != key {
		flushPromotionCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	promotionCache.row = &row
	promotionCache.isDirty = true
}

func getPromotionPar(key PromotionKey) uint64 {
	// Call partition function to determine shard
	return g(key.p_promo_sk)
}

func putPromotionPar(key PromotionKey) uint64 {
	// Call partition function to determine shard
	return g(key.p_promo_sk)
}

type ItemKey struct {
	i_item_sk uint64
}

type Item struct {
	Key ItemKey
	i_item_id string
	i_item_desc string
	i_current_price float32
	i_class string
	i_category string
	i_manufact string
}

// flushItemCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushItemCacheUnlocked(tx *bolt.Tx) {
	if itemCache.row != nil && itemCache.isDirty {
		bucket := tx.Bucket([]byte("tItem"))
		if bucket == nil {
			panic("missing bucket tItem")
		}
		var key ItemKey = itemCache.row.Key
		var row Item = *itemCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	itemCache.row = nil
	itemCache.isDirty = false
}

// flushItemCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushItemCache(tx *bolt.Tx) {
	itemCache.lock.Lock()
	defer itemCache.lock.Unlock()
	flushItemCacheUnlocked(tx)
}

// getItem fetches data, utilizing the cache in a highly concurrent and safe manner.
func getItem(tx *bolt.Tx, key ItemKey) ([]byte, Item) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	itemCache.lock.RLock()
	if itemCache.row != nil && itemCache.row.Key == key {
		row := *itemCache.row
		itemCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	itemCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	itemCache.lock.Lock()
	defer itemCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if itemCache.row != nil && itemCache.row.Key == key {
		return mustJSON(key), *itemCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushItemCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tItem"))
	if bucket == nil {
		panic("missing bucket tItem")
	}
	keyBytes := mustJSON(key)
	var row Item
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	itemCache.row = &row
	itemCache.isDirty = false

	return keyBytes, row
}

// putItem updates the cache and marks it as dirty (write-back).
func putItem(tx *bolt.Tx, key ItemKey, row Item) {
	itemCache.lock.Lock()
	defer itemCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if itemCache.row != nil && itemCache.row.Key != key {
		flushItemCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	itemCache.row = &row
	itemCache.isDirty = true
}

func getItemPar(key ItemKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.i_item_sk)
}

func putItemPar(key ItemKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.i_item_sk)
}

type CustomerKey struct {
	c_customer_sk uint64
}

type Customer struct {
	Key CustomerKey
	c_customer_id string
	c_first_name string
	c_last_name string
	c_birth_year uint64
	c_email_address string
}

// flushCustomerCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushCustomerCacheUnlocked(tx *bolt.Tx) {
	if customerCache.row != nil && customerCache.isDirty {
		bucket := tx.Bucket([]byte("tCustomer"))
		if bucket == nil {
			panic("missing bucket tCustomer")
		}
		var key CustomerKey = customerCache.row.Key
		var row Customer = *customerCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	customerCache.row = nil
	customerCache.isDirty = false
}

// flushCustomerCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushCustomerCache(tx *bolt.Tx) {
	customerCache.lock.Lock()
	defer customerCache.lock.Unlock()
	flushCustomerCacheUnlocked(tx)
}

// getCustomer fetches data, utilizing the cache in a highly concurrent and safe manner.
func getCustomer(tx *bolt.Tx, key CustomerKey) ([]byte, Customer) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	customerCache.lock.RLock()
	if customerCache.row != nil && customerCache.row.Key == key {
		row := *customerCache.row
		customerCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	customerCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	customerCache.lock.Lock()
	defer customerCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if customerCache.row != nil && customerCache.row.Key == key {
		return mustJSON(key), *customerCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushCustomerCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tCustomer"))
	if bucket == nil {
		panic("missing bucket tCustomer")
	}
	keyBytes := mustJSON(key)
	var row Customer
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	customerCache.row = &row
	customerCache.isDirty = false

	return keyBytes, row
}

// putCustomer updates the cache and marks it as dirty (write-back).
func putCustomer(tx *bolt.Tx, key CustomerKey, row Customer) {
	customerCache.lock.Lock()
	defer customerCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if customerCache.row != nil && customerCache.row.Key != key {
		flushCustomerCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	customerCache.row = &row
	customerCache.isDirty = true
}

func getCustomerPar(key CustomerKey) uint64 {
	// Call partition function to determine shard
	return f_cust(key.c_customer_sk)
}

func putCustomerPar(key CustomerKey) uint64 {
	// Call partition function to determine shard
	return f_cust(key.c_customer_sk)
}

type CustomerDemographicsKey struct {
	cd_demo_sk uint64
}

type CustomerDemographics struct {
	Key CustomerDemographicsKey
	cd_gender string
	cd_marital_status string
	cd_education_status string
	cd_purchase_estimate uint64
	cd_credit_rating string
}

// flushCustomerDemographicsCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushCustomerDemographicsCacheUnlocked(tx *bolt.Tx) {
	if customer_demographicsCache.row != nil && customer_demographicsCache.isDirty {
		bucket := tx.Bucket([]byte("tCustomerDemographics"))
		if bucket == nil {
			panic("missing bucket tCustomerDemographics")
		}
		var key CustomerDemographicsKey = customer_demographicsCache.row.Key
		var row CustomerDemographics = *customer_demographicsCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	customer_demographicsCache.row = nil
	customer_demographicsCache.isDirty = false
}

// flushCustomerDemographicsCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushCustomerDemographicsCache(tx *bolt.Tx) {
	customer_demographicsCache.lock.Lock()
	defer customer_demographicsCache.lock.Unlock()
	flushCustomerDemographicsCacheUnlocked(tx)
}

// getCustomerDemographics fetches data, utilizing the cache in a highly concurrent and safe manner.
func getCustomerDemographics(tx *bolt.Tx, key CustomerDemographicsKey) ([]byte, CustomerDemographics) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	customer_demographicsCache.lock.RLock()
	if customer_demographicsCache.row != nil && customer_demographicsCache.row.Key == key {
		row := *customer_demographicsCache.row
		customer_demographicsCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	customer_demographicsCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	customer_demographicsCache.lock.Lock()
	defer customer_demographicsCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if customer_demographicsCache.row != nil && customer_demographicsCache.row.Key == key {
		return mustJSON(key), *customer_demographicsCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushCustomerDemographicsCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tCustomerDemographics"))
	if bucket == nil {
		panic("missing bucket tCustomerDemographics")
	}
	keyBytes := mustJSON(key)
	var row CustomerDemographics
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	customer_demographicsCache.row = &row
	customer_demographicsCache.isDirty = false

	return keyBytes, row
}

// putCustomerDemographics updates the cache and marks it as dirty (write-back).
func putCustomerDemographics(tx *bolt.Tx, key CustomerDemographicsKey, row CustomerDemographics) {
	customer_demographicsCache.lock.Lock()
	defer customer_demographicsCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if customer_demographicsCache.row != nil && customer_demographicsCache.row.Key != key {
		flushCustomerDemographicsCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	customer_demographicsCache.row = &row
	customer_demographicsCache.isDirty = true
}

func getCustomerDemographicsPar(key CustomerDemographicsKey) uint64 {
	// Call partition function to determine shard
	return f_cust(key.cd_demo_sk)
}

func putCustomerDemographicsPar(key CustomerDemographicsKey) uint64 {
	// Call partition function to determine shard
	return f_cust(key.cd_demo_sk)
}

type CustomerAddressKey struct {
	ca_address_sk uint64
}

type CustomerAddress struct {
	Key CustomerAddressKey
	ca_street_number string
	ca_street_name string
	ca_city string
	ca_state string
	ca_zip string
	ca_country string
	ca_gmt_offset float32
}

// flushCustomerAddressCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushCustomerAddressCacheUnlocked(tx *bolt.Tx) {
	if customer_addressCache.row != nil && customer_addressCache.isDirty {
		bucket := tx.Bucket([]byte("tCustomerAddress"))
		if bucket == nil {
			panic("missing bucket tCustomerAddress")
		}
		var key CustomerAddressKey = customer_addressCache.row.Key
		var row CustomerAddress = *customer_addressCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	customer_addressCache.row = nil
	customer_addressCache.isDirty = false
}

// flushCustomerAddressCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushCustomerAddressCache(tx *bolt.Tx) {
	customer_addressCache.lock.Lock()
	defer customer_addressCache.lock.Unlock()
	flushCustomerAddressCacheUnlocked(tx)
}

// getCustomerAddress fetches data, utilizing the cache in a highly concurrent and safe manner.
func getCustomerAddress(tx *bolt.Tx, key CustomerAddressKey) ([]byte, CustomerAddress) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	customer_addressCache.lock.RLock()
	if customer_addressCache.row != nil && customer_addressCache.row.Key == key {
		row := *customer_addressCache.row
		customer_addressCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	customer_addressCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	customer_addressCache.lock.Lock()
	defer customer_addressCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if customer_addressCache.row != nil && customer_addressCache.row.Key == key {
		return mustJSON(key), *customer_addressCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushCustomerAddressCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tCustomerAddress"))
	if bucket == nil {
		panic("missing bucket tCustomerAddress")
	}
	keyBytes := mustJSON(key)
	var row CustomerAddress
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	customer_addressCache.row = &row
	customer_addressCache.isDirty = false

	return keyBytes, row
}

// putCustomerAddress updates the cache and marks it as dirty (write-back).
func putCustomerAddress(tx *bolt.Tx, key CustomerAddressKey, row CustomerAddress) {
	customer_addressCache.lock.Lock()
	defer customer_addressCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if customer_addressCache.row != nil && customer_addressCache.row.Key != key {
		flushCustomerAddressCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	customer_addressCache.row = &row
	customer_addressCache.isDirty = true
}

func getCustomerAddressPar(key CustomerAddressKey) uint64 {
	// Call partition function to determine shard
	return f_cust(key.ca_address_sk)
}

func putCustomerAddressPar(key CustomerAddressKey) uint64 {
	// Call partition function to determine shard
	return f_cust(key.ca_address_sk)
}

type StoreKey struct {
	s_store_sk uint64
}

type Store struct {
	Key StoreKey
	s_store_id string
	s_store_name string
	s_number_employees uint64
	s_state string
	s_country string
}

// flushStoreCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushStoreCacheUnlocked(tx *bolt.Tx) {
	if storeCache.row != nil && storeCache.isDirty {
		bucket := tx.Bucket([]byte("tStore"))
		if bucket == nil {
			panic("missing bucket tStore")
		}
		var key StoreKey = storeCache.row.Key
		var row Store = *storeCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	storeCache.row = nil
	storeCache.isDirty = false
}

// flushStoreCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushStoreCache(tx *bolt.Tx) {
	storeCache.lock.Lock()
	defer storeCache.lock.Unlock()
	flushStoreCacheUnlocked(tx)
}

// getStore fetches data, utilizing the cache in a highly concurrent and safe manner.
func getStore(tx *bolt.Tx, key StoreKey) ([]byte, Store) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	storeCache.lock.RLock()
	if storeCache.row != nil && storeCache.row.Key == key {
		row := *storeCache.row
		storeCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	storeCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	storeCache.lock.Lock()
	defer storeCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if storeCache.row != nil && storeCache.row.Key == key {
		return mustJSON(key), *storeCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushStoreCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tStore"))
	if bucket == nil {
		panic("missing bucket tStore")
	}
	keyBytes := mustJSON(key)
	var row Store
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	storeCache.row = &row
	storeCache.isDirty = false

	return keyBytes, row
}

// putStore updates the cache and marks it as dirty (write-back).
func putStore(tx *bolt.Tx, key StoreKey, row Store) {
	storeCache.lock.Lock()
	defer storeCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if storeCache.row != nil && storeCache.row.Key != key {
		flushStoreCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	storeCache.row = &row
	storeCache.isDirty = true
}

func getStorePar(key StoreKey) uint64 {
	// Call partition function to determine shard
	return f_store(key.s_store_sk)
}

func putStorePar(key StoreKey) uint64 {
	// Call partition function to determine shard
	return f_store(key.s_store_sk)
}

type WarehouseKey struct {
	w_warehouse_sk uint64
}

type Warehouse struct {
	Key WarehouseKey
	w_warehouse_name string
	w_state string
	w_country string
}

// flushWarehouseCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushWarehouseCacheUnlocked(tx *bolt.Tx) {
	if warehouseCache.row != nil && warehouseCache.isDirty {
		bucket := tx.Bucket([]byte("tWarehouse"))
		if bucket == nil {
			panic("missing bucket tWarehouse")
		}
		var key WarehouseKey = warehouseCache.row.Key
		var row Warehouse = *warehouseCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	warehouseCache.row = nil
	warehouseCache.isDirty = false
}

// flushWarehouseCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushWarehouseCache(tx *bolt.Tx) {
	warehouseCache.lock.Lock()
	defer warehouseCache.lock.Unlock()
	flushWarehouseCacheUnlocked(tx)
}

// getWarehouse fetches data, utilizing the cache in a highly concurrent and safe manner.
func getWarehouse(tx *bolt.Tx, key WarehouseKey) ([]byte, Warehouse) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	warehouseCache.lock.RLock()
	if warehouseCache.row != nil && warehouseCache.row.Key == key {
		row := *warehouseCache.row
		warehouseCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	warehouseCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	warehouseCache.lock.Lock()
	defer warehouseCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if warehouseCache.row != nil && warehouseCache.row.Key == key {
		return mustJSON(key), *warehouseCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushWarehouseCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tWarehouse"))
	if bucket == nil {
		panic("missing bucket tWarehouse")
	}
	keyBytes := mustJSON(key)
	var row Warehouse
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	warehouseCache.row = &row
	warehouseCache.isDirty = false

	return keyBytes, row
}

// putWarehouse updates the cache and marks it as dirty (write-back).
func putWarehouse(tx *bolt.Tx, key WarehouseKey, row Warehouse) {
	warehouseCache.lock.Lock()
	defer warehouseCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if warehouseCache.row != nil && warehouseCache.row.Key != key {
		flushWarehouseCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	warehouseCache.row = &row
	warehouseCache.isDirty = true
}

func getWarehousePar(key WarehouseKey) uint64 {
	// Call partition function to determine shard
	return f_store(key.w_warehouse_sk)
}

func putWarehousePar(key WarehouseKey) uint64 {
	// Call partition function to determine shard
	return f_store(key.w_warehouse_sk)
}

type StoreSalesKey struct {
	ss_item_sk uint64
	ss_ticket_number uint64
}

type StoreSales struct {
	Key StoreSalesKey
	ss_sold_date_sk uint64
	ss_customer_sk uint64
	ss_store_sk uint64
	ss_promo_sk uint64
	ss_quantity uint64
	ss_list_price float32
	ss_sales_price float32
	ss_ext_sales_price float32
	ss_net_paid float32
	ss_net_profit float32
}

// flushStoreSalesCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushStoreSalesCacheUnlocked(tx *bolt.Tx) {
	if store_salesCache.row != nil && store_salesCache.isDirty {
		bucket := tx.Bucket([]byte("tStoreSales"))
		if bucket == nil {
			panic("missing bucket tStoreSales")
		}
		var key StoreSalesKey = store_salesCache.row.Key
		var row StoreSales = *store_salesCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	store_salesCache.row = nil
	store_salesCache.isDirty = false
}

// flushStoreSalesCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushStoreSalesCache(tx *bolt.Tx) {
	store_salesCache.lock.Lock()
	defer store_salesCache.lock.Unlock()
	flushStoreSalesCacheUnlocked(tx)
}

// getStoreSales fetches data, utilizing the cache in a highly concurrent and safe manner.
func getStoreSales(tx *bolt.Tx, key StoreSalesKey) ([]byte, StoreSales) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	store_salesCache.lock.RLock()
	if store_salesCache.row != nil && store_salesCache.row.Key == key {
		row := *store_salesCache.row
		store_salesCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	store_salesCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	store_salesCache.lock.Lock()
	defer store_salesCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if store_salesCache.row != nil && store_salesCache.row.Key == key {
		return mustJSON(key), *store_salesCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushStoreSalesCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tStoreSales"))
	if bucket == nil {
		panic("missing bucket tStoreSales")
	}
	keyBytes := mustJSON(key)
	var row StoreSales
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	store_salesCache.row = &row
	store_salesCache.isDirty = false

	return keyBytes, row
}

// putStoreSales updates the cache and marks it as dirty (write-back).
func putStoreSales(tx *bolt.Tx, key StoreSalesKey, row StoreSales) {
	store_salesCache.lock.Lock()
	defer store_salesCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if store_salesCache.row != nil && store_salesCache.row.Key != key {
		flushStoreSalesCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	store_salesCache.row = &row
	store_salesCache.isDirty = true
}

func getStoreSalesPar(key StoreSalesKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.ss_item_sk)
}

func putStoreSalesPar(key StoreSalesKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.ss_item_sk)
}

type StoreReturnsKey struct {
	sr_item_sk uint64
	sr_ticket_number uint64
}

type StoreReturns struct {
	Key StoreReturnsKey
	sr_returned_date_sk uint64
	sr_customer_sk uint64
	sr_store_sk uint64
	sr_return_quantity uint64
	sr_return_amount float32
	sr_net_loss float32
}

// flushStoreReturnsCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushStoreReturnsCacheUnlocked(tx *bolt.Tx) {
	if store_returnsCache.row != nil && store_returnsCache.isDirty {
		bucket := tx.Bucket([]byte("tStoreReturns"))
		if bucket == nil {
			panic("missing bucket tStoreReturns")
		}
		var key StoreReturnsKey = store_returnsCache.row.Key
		var row StoreReturns = *store_returnsCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	store_returnsCache.row = nil
	store_returnsCache.isDirty = false
}

// flushStoreReturnsCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushStoreReturnsCache(tx *bolt.Tx) {
	store_returnsCache.lock.Lock()
	defer store_returnsCache.lock.Unlock()
	flushStoreReturnsCacheUnlocked(tx)
}

// getStoreReturns fetches data, utilizing the cache in a highly concurrent and safe manner.
func getStoreReturns(tx *bolt.Tx, key StoreReturnsKey) ([]byte, StoreReturns) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	store_returnsCache.lock.RLock()
	if store_returnsCache.row != nil && store_returnsCache.row.Key == key {
		row := *store_returnsCache.row
		store_returnsCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	store_returnsCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	store_returnsCache.lock.Lock()
	defer store_returnsCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if store_returnsCache.row != nil && store_returnsCache.row.Key == key {
		return mustJSON(key), *store_returnsCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushStoreReturnsCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tStoreReturns"))
	if bucket == nil {
		panic("missing bucket tStoreReturns")
	}
	keyBytes := mustJSON(key)
	var row StoreReturns
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	store_returnsCache.row = &row
	store_returnsCache.isDirty = false

	return keyBytes, row
}

// putStoreReturns updates the cache and marks it as dirty (write-back).
func putStoreReturns(tx *bolt.Tx, key StoreReturnsKey, row StoreReturns) {
	store_returnsCache.lock.Lock()
	defer store_returnsCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if store_returnsCache.row != nil && store_returnsCache.row.Key != key {
		flushStoreReturnsCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	store_returnsCache.row = &row
	store_returnsCache.isDirty = true
}

func getStoreReturnsPar(key StoreReturnsKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.sr_item_sk)
}

func putStoreReturnsPar(key StoreReturnsKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.sr_item_sk)
}

type CatalogSalesKey struct {
	cs_item_sk uint64
	cs_order_number uint64
}

type CatalogSales struct {
	Key CatalogSalesKey
	cs_sold_date_sk uint64
	cs_bill_customer_sk uint64
	cs_ship_customer_sk uint64
	cs_quantity uint64
	cs_list_price float32
	cs_sales_price float32
	cs_ext_sales_price float32
	cs_net_profit float32
}

// flushCatalogSalesCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushCatalogSalesCacheUnlocked(tx *bolt.Tx) {
	if catalog_salesCache.row != nil && catalog_salesCache.isDirty {
		bucket := tx.Bucket([]byte("tCatalogSales"))
		if bucket == nil {
			panic("missing bucket tCatalogSales")
		}
		var key CatalogSalesKey = catalog_salesCache.row.Key
		var row CatalogSales = *catalog_salesCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	catalog_salesCache.row = nil
	catalog_salesCache.isDirty = false
}

// flushCatalogSalesCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushCatalogSalesCache(tx *bolt.Tx) {
	catalog_salesCache.lock.Lock()
	defer catalog_salesCache.lock.Unlock()
	flushCatalogSalesCacheUnlocked(tx)
}

// getCatalogSales fetches data, utilizing the cache in a highly concurrent and safe manner.
func getCatalogSales(tx *bolt.Tx, key CatalogSalesKey) ([]byte, CatalogSales) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	catalog_salesCache.lock.RLock()
	if catalog_salesCache.row != nil && catalog_salesCache.row.Key == key {
		row := *catalog_salesCache.row
		catalog_salesCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	catalog_salesCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	catalog_salesCache.lock.Lock()
	defer catalog_salesCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if catalog_salesCache.row != nil && catalog_salesCache.row.Key == key {
		return mustJSON(key), *catalog_salesCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushCatalogSalesCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tCatalogSales"))
	if bucket == nil {
		panic("missing bucket tCatalogSales")
	}
	keyBytes := mustJSON(key)
	var row CatalogSales
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	catalog_salesCache.row = &row
	catalog_salesCache.isDirty = false

	return keyBytes, row
}

// putCatalogSales updates the cache and marks it as dirty (write-back).
func putCatalogSales(tx *bolt.Tx, key CatalogSalesKey, row CatalogSales) {
	catalog_salesCache.lock.Lock()
	defer catalog_salesCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if catalog_salesCache.row != nil && catalog_salesCache.row.Key != key {
		flushCatalogSalesCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	catalog_salesCache.row = &row
	catalog_salesCache.isDirty = true
}

func getCatalogSalesPar(key CatalogSalesKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.cs_item_sk)
}

func putCatalogSalesPar(key CatalogSalesKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.cs_item_sk)
}

type CatalogReturnsKey struct {
	cr_item_sk uint64
	cr_order_number uint64
}

type CatalogReturns struct {
	Key CatalogReturnsKey
	cr_returned_date_sk uint64
	cr_returning_customer_sk uint64
	cr_return_amount float32
	cr_net_loss float32
}

// flushCatalogReturnsCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushCatalogReturnsCacheUnlocked(tx *bolt.Tx) {
	if catalog_returnsCache.row != nil && catalog_returnsCache.isDirty {
		bucket := tx.Bucket([]byte("tCatalogReturns"))
		if bucket == nil {
			panic("missing bucket tCatalogReturns")
		}
		var key CatalogReturnsKey = catalog_returnsCache.row.Key
		var row CatalogReturns = *catalog_returnsCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	catalog_returnsCache.row = nil
	catalog_returnsCache.isDirty = false
}

// flushCatalogReturnsCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushCatalogReturnsCache(tx *bolt.Tx) {
	catalog_returnsCache.lock.Lock()
	defer catalog_returnsCache.lock.Unlock()
	flushCatalogReturnsCacheUnlocked(tx)
}

// getCatalogReturns fetches data, utilizing the cache in a highly concurrent and safe manner.
func getCatalogReturns(tx *bolt.Tx, key CatalogReturnsKey) ([]byte, CatalogReturns) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	catalog_returnsCache.lock.RLock()
	if catalog_returnsCache.row != nil && catalog_returnsCache.row.Key == key {
		row := *catalog_returnsCache.row
		catalog_returnsCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	catalog_returnsCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	catalog_returnsCache.lock.Lock()
	defer catalog_returnsCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if catalog_returnsCache.row != nil && catalog_returnsCache.row.Key == key {
		return mustJSON(key), *catalog_returnsCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushCatalogReturnsCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tCatalogReturns"))
	if bucket == nil {
		panic("missing bucket tCatalogReturns")
	}
	keyBytes := mustJSON(key)
	var row CatalogReturns
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	catalog_returnsCache.row = &row
	catalog_returnsCache.isDirty = false

	return keyBytes, row
}

// putCatalogReturns updates the cache and marks it as dirty (write-back).
func putCatalogReturns(tx *bolt.Tx, key CatalogReturnsKey, row CatalogReturns) {
	catalog_returnsCache.lock.Lock()
	defer catalog_returnsCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if catalog_returnsCache.row != nil && catalog_returnsCache.row.Key != key {
		flushCatalogReturnsCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	catalog_returnsCache.row = &row
	catalog_returnsCache.isDirty = true
}

func getCatalogReturnsPar(key CatalogReturnsKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.cr_item_sk)
}

func putCatalogReturnsPar(key CatalogReturnsKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.cr_item_sk)
}

type WebSalesKey struct {
	ws_item_sk uint64
	ws_order_number uint64
}

type WebSales struct {
	Key WebSalesKey
	ws_sold_date_sk uint64
	ws_bill_customer_sk uint64
	ws_quantity uint64
	ws_list_price float32
	ws_sales_price float32
	ws_ext_sales_price float32
	ws_net_profit float32
}

// flushWebSalesCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushWebSalesCacheUnlocked(tx *bolt.Tx) {
	if web_salesCache.row != nil && web_salesCache.isDirty {
		bucket := tx.Bucket([]byte("tWebSales"))
		if bucket == nil {
			panic("missing bucket tWebSales")
		}
		var key WebSalesKey = web_salesCache.row.Key
		var row WebSales = *web_salesCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	web_salesCache.row = nil
	web_salesCache.isDirty = false
}

// flushWebSalesCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushWebSalesCache(tx *bolt.Tx) {
	web_salesCache.lock.Lock()
	defer web_salesCache.lock.Unlock()
	flushWebSalesCacheUnlocked(tx)
}

// getWebSales fetches data, utilizing the cache in a highly concurrent and safe manner.
func getWebSales(tx *bolt.Tx, key WebSalesKey) ([]byte, WebSales) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	web_salesCache.lock.RLock()
	if web_salesCache.row != nil && web_salesCache.row.Key == key {
		row := *web_salesCache.row
		web_salesCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	web_salesCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	web_salesCache.lock.Lock()
	defer web_salesCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if web_salesCache.row != nil && web_salesCache.row.Key == key {
		return mustJSON(key), *web_salesCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushWebSalesCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tWebSales"))
	if bucket == nil {
		panic("missing bucket tWebSales")
	}
	keyBytes := mustJSON(key)
	var row WebSales
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	web_salesCache.row = &row
	web_salesCache.isDirty = false

	return keyBytes, row
}

// putWebSales updates the cache and marks it as dirty (write-back).
func putWebSales(tx *bolt.Tx, key WebSalesKey, row WebSales) {
	web_salesCache.lock.Lock()
	defer web_salesCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if web_salesCache.row != nil && web_salesCache.row.Key != key {
		flushWebSalesCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	web_salesCache.row = &row
	web_salesCache.isDirty = true
}

func getWebSalesPar(key WebSalesKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.ws_item_sk)
}

func putWebSalesPar(key WebSalesKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.ws_item_sk)
}

type WebReturnsKey struct {
	wr_item_sk uint64
	wr_order_number uint64
}

type WebReturns struct {
	Key WebReturnsKey
	wr_returned_date_sk uint64
	wr_returning_customer_sk uint64
	wr_return_amount float32
	wr_net_loss float32
}

// flushWebReturnsCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushWebReturnsCacheUnlocked(tx *bolt.Tx) {
	if web_returnsCache.row != nil && web_returnsCache.isDirty {
		bucket := tx.Bucket([]byte("tWebReturns"))
		if bucket == nil {
			panic("missing bucket tWebReturns")
		}
		var key WebReturnsKey = web_returnsCache.row.Key
		var row WebReturns = *web_returnsCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	web_returnsCache.row = nil
	web_returnsCache.isDirty = false
}

// flushWebReturnsCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushWebReturnsCache(tx *bolt.Tx) {
	web_returnsCache.lock.Lock()
	defer web_returnsCache.lock.Unlock()
	flushWebReturnsCacheUnlocked(tx)
}

// getWebReturns fetches data, utilizing the cache in a highly concurrent and safe manner.
func getWebReturns(tx *bolt.Tx, key WebReturnsKey) ([]byte, WebReturns) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	web_returnsCache.lock.RLock()
	if web_returnsCache.row != nil && web_returnsCache.row.Key == key {
		row := *web_returnsCache.row
		web_returnsCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	web_returnsCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	web_returnsCache.lock.Lock()
	defer web_returnsCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if web_returnsCache.row != nil && web_returnsCache.row.Key == key {
		return mustJSON(key), *web_returnsCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushWebReturnsCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tWebReturns"))
	if bucket == nil {
		panic("missing bucket tWebReturns")
	}
	keyBytes := mustJSON(key)
	var row WebReturns
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	web_returnsCache.row = &row
	web_returnsCache.isDirty = false

	return keyBytes, row
}

// putWebReturns updates the cache and marks it as dirty (write-back).
func putWebReturns(tx *bolt.Tx, key WebReturnsKey, row WebReturns) {
	web_returnsCache.lock.Lock()
	defer web_returnsCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if web_returnsCache.row != nil && web_returnsCache.row.Key != key {
		flushWebReturnsCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	web_returnsCache.row = &row
	web_returnsCache.isDirty = true
}

func getWebReturnsPar(key WebReturnsKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.wr_item_sk)
}

func putWebReturnsPar(key WebReturnsKey) uint64 {
	// Call partition function to determine shard
	return f_item(key.wr_item_sk)
}

func f_item(x uint64) uint64 {
	goto BB0

BB0:
	return x
}

func f_cust(x uint64) uint64 {
	goto BB1

BB1:
	return x
}

func f_store(x uint64) uint64 {
	goto BB2

BB2:
	return x
}

func g(x uint64) uint64 {
	goto BB3

BB3:
	return 0
}

func Scheds() {
	// Initialize scheduler maps
	LockSchedulers = make(map[string]*Scheduler)
	AccessSchedulers = make(map[string]*Scheduler)
	AccessDSchedulers = make(map[string]*Scheduler)

	// Create one lock scheduler for each table
	LockSchedulers["DateDim"] = NewScheduler()
	LockSchedulers["TimeDim"] = NewScheduler()
	LockSchedulers["Promotion"] = NewScheduler()
	LockSchedulers["Item"] = NewScheduler()
	LockSchedulers["Customer"] = NewScheduler()
	LockSchedulers["CustomerDemographics"] = NewScheduler()
	LockSchedulers["CustomerAddress"] = NewScheduler()
	LockSchedulers["Store"] = NewScheduler()
	LockSchedulers["Warehouse"] = NewScheduler()
	LockSchedulers["StoreSales"] = NewScheduler()
	LockSchedulers["StoreReturns"] = NewScheduler()
	LockSchedulers["CatalogSales"] = NewScheduler()
	LockSchedulers["CatalogReturns"] = NewScheduler()
	LockSchedulers["WebSales"] = NewScheduler()
	LockSchedulers["WebReturns"] = NewScheduler()

}

func Chains(db *bolt.DB) []*Chain {
	return []*Chain{
		Q1ChainImpl(db),
		Q7ChainImpl(db),
		Q12ChainImpl(db),
		Q19ChainImpl(db),
		Q34ChainImpl(db),
		Q50ChainImpl(db),
		Q96ChainImpl(db),
	}
}

