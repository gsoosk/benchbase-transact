// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type Q96Chain struct {
	db   *bolt.DB
	hops []*Hop
}

// Q96Hop0 lowers hop 0 containing 1 basic block(s).
func Q96Hop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var item_sk uint64
	var order_num uint64
	var ws_ws_quantity uint64
	var qty uint64
	var _tmp50 Unit

	var keyBytes1 []byte
	var row1 WebSales

	item_sk = toUint64(params["item_sk"])
	order_num = toUint64(params["order_num"])

	goto BB25

BB25:
	// TableGet: WebSales
	keyBytes1, row1 = getWebSales(tx, WebSalesKey{ws_item_sk: item_sk, ws_order_number: order_num})
	rwSet = AddRWSet(rwSet, "WebSales", keyBytes1)
	ws_ws_quantity = row1.ws_quantity
	qty = ws_ws_quantity
	_ = qty
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// Q96Hop1 lowers hop 1 containing 1 basic block(s).
func Q96Hop1(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var warehouse_sk uint64
	var w_w_warehouse_name string
	var wname string
	var _tmp52 Unit

	var keyBytes1 []byte
	var row1 Warehouse

	warehouse_sk = toUint64(params["warehouse_sk"])

	goto BB26

BB26:
	// TableGet: Warehouse
	keyBytes1, row1 = getWarehouse(tx, WarehouseKey{w_warehouse_sk: warehouse_sk})
	rwSet = AddRWSet(rwSet, "Warehouse", keyBytes1)
	w_w_warehouse_name = row1.w_warehouse_name
	wname = w_w_warehouse_name
	_ = wname
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// Q96Hop2 lowers hop 2 containing 1 basic block(s).
func Q96Hop2(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var time_sk uint64
	var t_t_hour uint64
	var hr uint64
	var _tmp54 Unit

	var keyBytes1 []byte
	var row1 TimeDim

	time_sk = toUint64(params["time_sk"])

	goto BB27

BB27:
	// TableGet: TimeDim
	keyBytes1, row1 = getTimeDim(tx, TimeDimKey{t_time_sk: time_sk})
	rwSet = AddRWSet(rwSet, "TimeDim", keyBytes1)
	t_t_hour = row1.t_hour
	hr = t_t_hour
	_ = hr
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// Q96Hop3 lowers hop 3 containing 1 basic block(s).
func Q96Hop3(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var date_sk uint64
	var d_d_year uint64
	var yr uint64
	var _tmp56 Unit

	var keyBytes1 []byte
	var row1 DateDim

	date_sk = toUint64(params["date_sk"])

	goto BB28

BB28:
	// TableGet: DateDim
	keyBytes1, row1 = getDateDim(tx, DateDimKey{d_date_sk: date_sk})
	rwSet = AddRWSet(rwSet, "DateDim", keyBytes1)
	d_d_year = row1.d_year
	yr = d_d_year
	_ = yr
	// return - no action
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// Q96Hop0Par calculates the partition for hop 0 without database access.
func Q96Hop0Par(params map[string]string) uint64 {
	var item_sk uint64
	var order_num uint64
	var ws_ws_quantity uint64
	var qty uint64
	var _tmp50 Unit

	var keyBytes1 []byte
	var row1 WebSales

	item_sk = toUint64(params["item_sk"])
	order_num = toUint64(params["order_num"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB25

BB25:
	// First table access - calculate partition: WebSales
	if true { return getWebSalesPar(WebSalesKey{ws_item_sk: item_sk, ws_order_number: order_num}) }
	// TableGet: WebSales
	keyBytes1, row1 = getWebSales(tx, WebSalesKey{ws_item_sk: item_sk, ws_order_number: order_num})
	rwSet = AddRWSet(rwSet, "WebSales", keyBytes1)
	ws_ws_quantity = row1.ws_quantity
	qty = ws_ws_quantity
	_ = qty
	panic("unexpected hop exit in partition")
}

// Q96Hop1Par calculates the partition for hop 1 without database access.
func Q96Hop1Par(params map[string]string) uint64 {
	var warehouse_sk uint64
	var w_w_warehouse_name string
	var wname string
	var _tmp52 Unit

	var keyBytes1 []byte
	var row1 Warehouse

	warehouse_sk = toUint64(params["warehouse_sk"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB26

BB26:
	// First table access - calculate partition: Warehouse
	if true { return getWarehousePar(WarehouseKey{w_warehouse_sk: warehouse_sk}) }
	// TableGet: Warehouse
	keyBytes1, row1 = getWarehouse(tx, WarehouseKey{w_warehouse_sk: warehouse_sk})
	rwSet = AddRWSet(rwSet, "Warehouse", keyBytes1)
	w_w_warehouse_name = row1.w_warehouse_name
	wname = w_w_warehouse_name
	_ = wname
	panic("unexpected hop exit in partition")
}

// Q96Hop2Par calculates the partition for hop 2 without database access.
func Q96Hop2Par(params map[string]string) uint64 {
	var time_sk uint64
	var t_t_hour uint64
	var hr uint64
	var _tmp54 Unit

	var keyBytes1 []byte
	var row1 TimeDim

	time_sk = toUint64(params["time_sk"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB27

BB27:
	// First table access - calculate partition: TimeDim
	if true { return getTimeDimPar(TimeDimKey{t_time_sk: time_sk}) }
	// TableGet: TimeDim
	keyBytes1, row1 = getTimeDim(tx, TimeDimKey{t_time_sk: time_sk})
	rwSet = AddRWSet(rwSet, "TimeDim", keyBytes1)
	t_t_hour = row1.t_hour
	hr = t_t_hour
	_ = hr
	panic("unexpected hop exit in partition")
}

// Q96Hop3Par calculates the partition for hop 3 without database access.
func Q96Hop3Par(params map[string]string) uint64 {
	var date_sk uint64
	var d_d_year uint64
	var yr uint64
	var _tmp56 Unit

	var keyBytes1 []byte
	var row1 DateDim

	date_sk = toUint64(params["date_sk"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB28

BB28:
	// First table access - calculate partition: DateDim
	if true { return getDateDimPar(DateDimKey{d_date_sk: date_sk}) }
	// TableGet: DateDim
	keyBytes1, row1 = getDateDim(tx, DateDimKey{d_date_sk: date_sk})
	rwSet = AddRWSet(rwSet, "DateDim", keyBytes1)
	d_d_year = row1.d_year
	yr = d_d_year
	_ = yr
	return 0
}

func Q96NextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(Q96Hop0Par(paramsCopy))
	case 1:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(Q96Hop1Par(paramsCopy))
	case 2:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(Q96Hop2Par(paramsCopy))
	case 3:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(Q96Hop3Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func Q96ChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 4)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: true,
		process:    Q96Hop0,
		conflicts:  map[int32]int32{},
	}

	hops[1] = &Hop{
		id:         1,
		hopType:    util.NormalHop,
		isReadOnly: true,
		process:    Q96Hop1,
		conflicts:  map[int32]int32{},
	}

	hops[2] = &Hop{
		id:         2,
		hopType:    util.NormalHop,
		isReadOnly: true,
		process:    Q96Hop2,
		conflicts:  map[int32]int32{},
	}

	hops[3] = &Hop{
		id:         3,
		hopType:    util.NormalHop,
		isReadOnly: true,
		process:    Q96Hop3,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: Q96NextReq}
}

// legacy: use json package to avoid unused import error
func Q96ToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

