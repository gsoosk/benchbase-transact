// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"sync"
	"github.com/boltdb/bolt"
)

func mustJSON(value any) []byte {
	bytes, err := json.Marshal(value)
	if err != nil {
		panic(err)
	}
	return bytes
}

// Global variable for the single-row write-back cache with a RWMutex.
var subscriberCache struct {
	lock    sync.RWMutex
	row     *Subscriber
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var accessinfoCache struct {
	lock    sync.RWMutex
	row     *Accessinfo
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var specialfacilityCache struct {
	lock    sync.RWMutex
	row     *Specialfacility
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var callforwardingCache struct {
	lock    sync.RWMutex
	row     *Callforwarding
	isDirty bool
}

// FlushAllCaches flushes all table caches
func FlushAllCaches(tx *bolt.Tx) {
	flushSubscriberCache(tx)
	flushAccessinfoCache(tx)
	flushSpecialfacilityCache(tx)
	flushCallforwardingCache(tx)
}

// Iterator getter functions for table primary keys
// Generated for each primary key field in each table

// get_s_id_23 extracts s_id from the iterator for table Subscriber
func get_s_id_23(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_s_id_23: iterator is not valid")
	}

	// Deserialize the composite key to extract s_id
	type CompositeKey struct {
		s_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.s_id
}

// get_s_id_24 extracts s_id from the iterator for table Accessinfo
func get_s_id_24(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_s_id_24: iterator is not valid")
	}

	// Deserialize the composite key to extract s_id
	type CompositeKey struct {
		s_id uint64
		ai_type uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.s_id
}

// get_ai_type_25 extracts ai_type from the iterator for table Accessinfo
func get_ai_type_25(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ai_type_25: iterator is not valid")
	}

	// Deserialize the composite key to extract ai_type
	type CompositeKey struct {
		s_id uint64
		ai_type uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ai_type
}

// get_s_id_26 extracts s_id from the iterator for table Specialfacility
func get_s_id_26(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_s_id_26: iterator is not valid")
	}

	// Deserialize the composite key to extract s_id
	type CompositeKey struct {
		s_id uint64
		sf_type uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.s_id
}

// get_sf_type_27 extracts sf_type from the iterator for table Specialfacility
func get_sf_type_27(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_sf_type_27: iterator is not valid")
	}

	// Deserialize the composite key to extract sf_type
	type CompositeKey struct {
		s_id uint64
		sf_type uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.sf_type
}

// get_s_id_28 extracts s_id from the iterator for table Callforwarding
func get_s_id_28(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_s_id_28: iterator is not valid")
	}

	// Deserialize the composite key to extract s_id
	type CompositeKey struct {
		s_id uint64
		sf_type uint64
		start_time uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.s_id
}

// get_sf_type_29 extracts sf_type from the iterator for table Callforwarding
func get_sf_type_29(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_sf_type_29: iterator is not valid")
	}

	// Deserialize the composite key to extract sf_type
	type CompositeKey struct {
		s_id uint64
		sf_type uint64
		start_time uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.sf_type
}

// get_start_time_30 extracts start_time from the iterator for table Callforwarding
func get_start_time_30(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_start_time_30: iterator is not valid")
	}

	// Deserialize the composite key to extract start_time
	type CompositeKey struct {
		s_id uint64
		sf_type uint64
		start_time uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.start_time
}

const (
	__slice__ uint64 = 0
	__shards__ uint64 = 1
)

type SubscriberKey struct {
	s_id uint64
}

type Subscriber struct {
	Key SubscriberKey
	sub_nbr string
	bit_1 uint64
	bit_2 uint64
	bit_3 uint64
	bit_4 uint64
	bit_5 uint64
	bit_6 uint64
	bit_7 uint64
	bit_8 uint64
	bit_9 uint64
	bit_10 uint64
	hex_1 uint64
	hex_2 uint64
	hex_3 uint64
	hex_4 uint64
	hex_5 uint64
	hex_6 uint64
	hex_7 uint64
	hex_8 uint64
	hex_9 uint64
	hex_10 uint64
	byte2_1 uint64
	byte2_2 uint64
	byte2_3 uint64
	byte2_4 uint64
	byte2_5 uint64
	byte2_6 uint64
	byte2_7 uint64
	byte2_8 uint64
	byte2_9 uint64
	byte2_10 uint64
	msc_location uint64
	vlr_location uint64
}

// flushSubscriberCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushSubscriberCacheUnlocked(tx *bolt.Tx) {
	if subscriberCache.row != nil && subscriberCache.isDirty {
		bucket := tx.Bucket([]byte("tSubscriber"))
		if bucket == nil {
			panic("missing bucket tSubscriber")
		}
		var key SubscriberKey = subscriberCache.row.Key
		var row Subscriber = *subscriberCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	subscriberCache.row = nil
	subscriberCache.isDirty = false
}

// flushSubscriberCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushSubscriberCache(tx *bolt.Tx) {
	subscriberCache.lock.Lock()
	defer subscriberCache.lock.Unlock()
	flushSubscriberCacheUnlocked(tx)
}

// getSubscriber fetches data, utilizing the cache in a highly concurrent and safe manner.
func getSubscriber(tx *bolt.Tx, key SubscriberKey) ([]byte, Subscriber) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	subscriberCache.lock.RLock()
	if subscriberCache.row != nil && subscriberCache.row.Key == key {
		row := *subscriberCache.row
		subscriberCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	subscriberCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	subscriberCache.lock.Lock()
	defer subscriberCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if subscriberCache.row != nil && subscriberCache.row.Key == key {
		return mustJSON(key), *subscriberCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushSubscriberCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tSubscriber"))
	if bucket == nil {
		panic("missing bucket tSubscriber")
	}
	keyBytes := mustJSON(key)
	var row Subscriber
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	subscriberCache.row = &row
	subscriberCache.isDirty = false

	return keyBytes, row
}

// putSubscriber updates the cache and marks it as dirty (write-back).
func putSubscriber(tx *bolt.Tx, key SubscriberKey, row Subscriber) {
	subscriberCache.lock.Lock()
	defer subscriberCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if subscriberCache.row != nil && subscriberCache.row.Key != key {
		flushSubscriberCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	subscriberCache.row = &row
	subscriberCache.isDirty = true
}

func getSubscriberPar(key SubscriberKey) uint64 {
	// Call partition function to determine shard
	return subPartition(key.s_id)
}

func putSubscriberPar(key SubscriberKey) uint64 {
	// Call partition function to determine shard
	return subPartition(key.s_id)
}

type AccessinfoKey struct {
	s_id uint64
	ai_type uint64
}

type Accessinfo struct {
	Key AccessinfoKey
	data1 uint64
	data2 uint64
	data3 string
	data4 string
}

// flushAccessinfoCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushAccessinfoCacheUnlocked(tx *bolt.Tx) {
	if accessinfoCache.row != nil && accessinfoCache.isDirty {
		bucket := tx.Bucket([]byte("tAccessinfo"))
		if bucket == nil {
			panic("missing bucket tAccessinfo")
		}
		var key AccessinfoKey = accessinfoCache.row.Key
		var row Accessinfo = *accessinfoCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	accessinfoCache.row = nil
	accessinfoCache.isDirty = false
}

// flushAccessinfoCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushAccessinfoCache(tx *bolt.Tx) {
	accessinfoCache.lock.Lock()
	defer accessinfoCache.lock.Unlock()
	flushAccessinfoCacheUnlocked(tx)
}

// getAccessinfo fetches data, utilizing the cache in a highly concurrent and safe manner.
func getAccessinfo(tx *bolt.Tx, key AccessinfoKey) ([]byte, Accessinfo) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	accessinfoCache.lock.RLock()
	if accessinfoCache.row != nil && accessinfoCache.row.Key == key {
		row := *accessinfoCache.row
		accessinfoCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	accessinfoCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	accessinfoCache.lock.Lock()
	defer accessinfoCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if accessinfoCache.row != nil && accessinfoCache.row.Key == key {
		return mustJSON(key), *accessinfoCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushAccessinfoCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tAccessinfo"))
	if bucket == nil {
		panic("missing bucket tAccessinfo")
	}
	keyBytes := mustJSON(key)
	var row Accessinfo
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	accessinfoCache.row = &row
	accessinfoCache.isDirty = false

	return keyBytes, row
}

// putAccessinfo updates the cache and marks it as dirty (write-back).
func putAccessinfo(tx *bolt.Tx, key AccessinfoKey, row Accessinfo) {
	accessinfoCache.lock.Lock()
	defer accessinfoCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if accessinfoCache.row != nil && accessinfoCache.row.Key != key {
		flushAccessinfoCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	accessinfoCache.row = &row
	accessinfoCache.isDirty = true
}

func getAccessinfoPar(key AccessinfoKey) uint64 {
	// Call partition function to determine shard
	return subPartition(key.s_id)
}

func putAccessinfoPar(key AccessinfoKey) uint64 {
	// Call partition function to determine shard
	return subPartition(key.s_id)
}

type SpecialfacilityKey struct {
	s_id uint64
	sf_type uint64
}

type Specialfacility struct {
	Key SpecialfacilityKey
	is_active uint64
	error_cntrl uint64
	data_a uint64
	data_b string
}

// flushSpecialfacilityCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushSpecialfacilityCacheUnlocked(tx *bolt.Tx) {
	if specialfacilityCache.row != nil && specialfacilityCache.isDirty {
		bucket := tx.Bucket([]byte("tSpecialfacility"))
		if bucket == nil {
			panic("missing bucket tSpecialfacility")
		}
		var key SpecialfacilityKey = specialfacilityCache.row.Key
		var row Specialfacility = *specialfacilityCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	specialfacilityCache.row = nil
	specialfacilityCache.isDirty = false
}

// flushSpecialfacilityCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushSpecialfacilityCache(tx *bolt.Tx) {
	specialfacilityCache.lock.Lock()
	defer specialfacilityCache.lock.Unlock()
	flushSpecialfacilityCacheUnlocked(tx)
}

// getSpecialfacility fetches data, utilizing the cache in a highly concurrent and safe manner.
func getSpecialfacility(tx *bolt.Tx, key SpecialfacilityKey) ([]byte, Specialfacility) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	specialfacilityCache.lock.RLock()
	if specialfacilityCache.row != nil && specialfacilityCache.row.Key == key {
		row := *specialfacilityCache.row
		specialfacilityCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	specialfacilityCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	specialfacilityCache.lock.Lock()
	defer specialfacilityCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if specialfacilityCache.row != nil && specialfacilityCache.row.Key == key {
		return mustJSON(key), *specialfacilityCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushSpecialfacilityCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tSpecialfacility"))
	if bucket == nil {
		panic("missing bucket tSpecialfacility")
	}
	keyBytes := mustJSON(key)
	var row Specialfacility
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	specialfacilityCache.row = &row
	specialfacilityCache.isDirty = false

	return keyBytes, row
}

// putSpecialfacility updates the cache and marks it as dirty (write-back).
func putSpecialfacility(tx *bolt.Tx, key SpecialfacilityKey, row Specialfacility) {
	specialfacilityCache.lock.Lock()
	defer specialfacilityCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if specialfacilityCache.row != nil && specialfacilityCache.row.Key != key {
		flushSpecialfacilityCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	specialfacilityCache.row = &row
	specialfacilityCache.isDirty = true
}

func getSpecialfacilityPar(key SpecialfacilityKey) uint64 {
	// Call partition function to determine shard
	return subPartition(key.s_id)
}

func putSpecialfacilityPar(key SpecialfacilityKey) uint64 {
	// Call partition function to determine shard
	return subPartition(key.s_id)
}

type CallforwardingKey struct {
	s_id uint64
	sf_type uint64
	start_time uint64
}

type Callforwarding struct {
	Key CallforwardingKey
	end_time uint64
	numberx string
}

// flushCallforwardingCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushCallforwardingCacheUnlocked(tx *bolt.Tx) {
	if callforwardingCache.row != nil && callforwardingCache.isDirty {
		bucket := tx.Bucket([]byte("tCallforwarding"))
		if bucket == nil {
			panic("missing bucket tCallforwarding")
		}
		var key CallforwardingKey = callforwardingCache.row.Key
		var row Callforwarding = *callforwardingCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	callforwardingCache.row = nil
	callforwardingCache.isDirty = false
}

// flushCallforwardingCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushCallforwardingCache(tx *bolt.Tx) {
	callforwardingCache.lock.Lock()
	defer callforwardingCache.lock.Unlock()
	flushCallforwardingCacheUnlocked(tx)
}

// getCallforwarding fetches data, utilizing the cache in a highly concurrent and safe manner.
func getCallforwarding(tx *bolt.Tx, key CallforwardingKey) ([]byte, Callforwarding) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	callforwardingCache.lock.RLock()
	if callforwardingCache.row != nil && callforwardingCache.row.Key == key {
		row := *callforwardingCache.row
		callforwardingCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	callforwardingCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	callforwardingCache.lock.Lock()
	defer callforwardingCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if callforwardingCache.row != nil && callforwardingCache.row.Key == key {
		return mustJSON(key), *callforwardingCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushCallforwardingCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tCallforwarding"))
	if bucket == nil {
		panic("missing bucket tCallforwarding")
	}
	keyBytes := mustJSON(key)
	var row Callforwarding
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	callforwardingCache.row = &row
	callforwardingCache.isDirty = false

	return keyBytes, row
}

// putCallforwarding updates the cache and marks it as dirty (write-back).
func putCallforwarding(tx *bolt.Tx, key CallforwardingKey, row Callforwarding) {
	callforwardingCache.lock.Lock()
	defer callforwardingCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if callforwardingCache.row != nil && callforwardingCache.row.Key != key {
		flushCallforwardingCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	callforwardingCache.row = &row
	callforwardingCache.isDirty = true
}

func getCallforwardingPar(key CallforwardingKey) uint64 {
	// Call partition function to determine shard
	return subPartition(key.s_id)
}

func putCallforwardingPar(key CallforwardingKey) uint64 {
	// Call partition function to determine shard
	return subPartition(key.s_id)
}

func subPartition(s_id uint64) uint64 {
	goto BB0

BB0:
	return s_id
}

func Scheds() {
	// Initialize scheduler maps
	LockSchedulers = make(map[string]*Scheduler)
	AccessSchedulers = make(map[string]*Scheduler)
	AccessDSchedulers = make(map[string]*Scheduler)

	// Create one lock scheduler for each table
	LockSchedulers["Subscriber"] = NewScheduler()
	LockSchedulers["Accessinfo"] = NewScheduler()
	LockSchedulers["Specialfacility"] = NewScheduler()
	LockSchedulers["Callforwarding"] = NewScheduler()

}

func Chains(db *bolt.DB) []*Chain {
	return []*Chain{
		DeletecallforwardingChainImpl(db),
		GetaccessdataChainImpl(db),
		GetnewdestinationChainImpl(db),
		GetsubscriberdataChainImpl(db),
		InsertcallforwardingChainImpl(db),
		UpdatelocationChainImpl(db),
		UpdatesubscriberdataChainImpl(db),
	}
}

