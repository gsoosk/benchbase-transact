// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type VoteChain struct {
	db   *bolt.DB
	hops []*Hop
}

// VoteHop0 lowers hop 0 containing 1 basic block(s).
func VoteHop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var contestantNumber uint64
	var c_contestant_name string
	var cname string
	var _tmp1 Unit

	var keyBytes1 []byte
	var row1 Contestants

	contestantNumber = toUint64(params["contestantNumber"])

	goto BB3

BB3:
	// TableGet: Contestants
	keyBytes1, row1 = getContestants(tx, ContestantsKey{contestant_number: contestantNumber})
	rwSet = AddRWSet(rwSet, "Contestants", keyBytes1)
	c_contestant_name = row1.contestant_name
	cname = c_contestant_name
	_ = cname
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// VoteHop1 lowers hop 1 containing 1 basic block(s).
func VoteHop1(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var phoneNumber uint64
	var area_code uint64
	var _tmp2 uint64
	var loc_state string
	var state string

	var keyBytes1 []byte
	var row1 Locations

	phoneNumber = toUint64(params["phoneNumber"])

	goto BB4

BB4:
	_tmp2 = phoneNumber / 10000000
	area_code = _tmp2
	// TableGet: Locations
	keyBytes1, row1 = getLocations(tx, LocationsKey{area_code: area_code})
	rwSet = AddRWSet(rwSet, "Locations", keyBytes1)
	loc_state = row1.state
	state = loc_state
	// Write back variables to results
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		Results: []string{state},
		RWSets: rwSet,
	}, nil
}

// VoteHop2 lowers hop 2 containing 1 basic block(s).
func VoteHop2(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var voteId uint64
	var phoneNumber uint64
	var contestantNumber uint64
	var state string

	var keyBytes1 []byte
	var row1 Votes

	voteId = toUint64(params["voteId"])
	phoneNumber = toUint64(params["phoneNumber"])
	contestantNumber = toUint64(params["contestantNumber"])
	state = params["state"]

	goto BB5

BB5:
	// Combined table access: Votes (2 operations)
	keyBytes1, row1 = getVotes(tx, VotesKey{phone_number: phoneNumber, vote_id: voteId})
	rwSet = AddRWSet(rwSet, "Votes", keyBytes1)
	row1.state = state
	row1.contestant_number = contestantNumber
	putVotes(tx, VotesKey{phone_number: phoneNumber, vote_id: voteId}, row1)
	// return - no action
	// Flush caches for tables written in this hop
	flushVotesCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// VoteHop0Par calculates the partition for hop 0 without database access.
func VoteHop0Par(params map[string]string) uint64 {
	var contestantNumber uint64
	var c_contestant_name string
	var cname string
	var _tmp1 Unit

	var keyBytes1 []byte
	var row1 Contestants

	contestantNumber = toUint64(params["contestantNumber"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB3

BB3:
	// First table access - calculate partition: Contestants
	if true { return getContestantsPar(ContestantsKey{contestant_number: contestantNumber}) }
	// TableGet: Contestants
	keyBytes1, row1 = getContestants(tx, ContestantsKey{contestant_number: contestantNumber})
	rwSet = AddRWSet(rwSet, "Contestants", keyBytes1)
	c_contestant_name = row1.contestant_name
	cname = c_contestant_name
	_ = cname
	panic("unexpected hop exit in partition")
}

// VoteHop1Par calculates the partition for hop 1 without database access.
func VoteHop1Par(params map[string]string) uint64 {
	var phoneNumber uint64
	var area_code uint64
	var _tmp2 uint64
	var loc_state string
	var state string

	var keyBytes1 []byte
	var row1 Locations

	phoneNumber = toUint64(params["phoneNumber"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB4

BB4:
	_tmp2 = phoneNumber / 10000000
	area_code = _tmp2
	// First table access - calculate partition: Locations
	if true { return getLocationsPar(LocationsKey{area_code: area_code}) }
	// TableGet: Locations
	keyBytes1, row1 = getLocations(tx, LocationsKey{area_code: area_code})
	rwSet = AddRWSet(rwSet, "Locations", keyBytes1)
	loc_state = row1.state
	state = loc_state
	// Unreachable: use variables in results
	_ = []string{state}
	panic("unexpected hop exit in partition")
}

// VoteHop2Par calculates the partition for hop 2 without database access.
func VoteHop2Par(params map[string]string) uint64 {
	var voteId uint64
	var phoneNumber uint64
	var contestantNumber uint64
	var state string

	var keyBytes1 []byte
	var row1 Votes

	voteId = toUint64(params["voteId"])
	phoneNumber = toUint64(params["phoneNumber"])
	contestantNumber = toUint64(params["contestantNumber"])
	state = params["state"]

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB5

BB5:
	// First table access (optimized group) - calculate partition: Votes
	if true { return putVotesPar(VotesKey{phone_number: phoneNumber, vote_id: voteId}) }
	// Combined table access: Votes (2 operations)
	keyBytes1, row1 = getVotes(tx, VotesKey{phone_number: phoneNumber, vote_id: voteId})
	rwSet = AddRWSet(rwSet, "Votes", keyBytes1)
	row1.state = state
	row1.contestant_number = contestantNumber
	putVotes(tx, VotesKey{phone_number: phoneNumber, vote_id: voteId}, row1)
	return 0
}

func VoteNextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(VoteHop0Par(paramsCopy))
	case 1:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(VoteHop1Par(paramsCopy))
	case 2:
		// Extract variables from previous hop results
		params["state"] = in.Results[0]
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(VoteHop2Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func VoteChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 3)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: true,
		process:    VoteHop0,
		conflicts:  map[int32]int32{},
	}

	hops[1] = &Hop{
		id:         1,
		hopType:    util.NormalHop,
		isReadOnly: true,
		process:    VoteHop1,
		conflicts:  map[int32]int32{},
	}

	hops[2] = &Hop{
		id:         2,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    VoteHop2,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: VoteNextReq}
}

// legacy: use json package to avoid unused import error
func VoteToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

