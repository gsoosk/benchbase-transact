// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type SendpaymentChain struct {
	db   *bolt.DB
	hops []*Hop
}

// SendpaymentHop0 lowers hop 0 containing 1 basic block(s).
func SendpaymentHop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var sendAcct uint64
	var amount float32
	var c0_custid uint64
	var c0_bal float32
	var _tmp13 float32

	var keyBytes1 []byte
	var row1 Checking
	var keyBytes2 []byte
	var row2 Checking

	sendAcct = toUint64(params["sendAcct"])
	amount = toFloat32(params["amount"])

	goto BB5

BB5:
	// Combined table access: Checking (2 operations)
	keyBytes1, row1 = getChecking(tx, CheckingKey{custid: sendAcct})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes1)
	c0_bal = row1.bal
	c0_custid = row1.Key.custid
	_tmp13 = c0_bal - amount
	c0_bal = _tmp13
	// Combined table access: Checking (2 operations)
	keyBytes2, row2 = getChecking(tx, CheckingKey{custid: sendAcct})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes2)
	row2.bal = c0_bal
	row2.Key.custid = c0_custid
	putChecking(tx, CheckingKey{custid: sendAcct}, row2)
	// Flush caches for tables written in this hop
	flushCheckingCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// SendpaymentHop1 lowers hop 1 containing 1 basic block(s).
func SendpaymentHop1(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var destAcct uint64
	var amount float32
	var c1_custid uint64
	var c1_bal float32
	var _tmp15 float32

	var keyBytes1 []byte
	var row1 Checking
	var keyBytes2 []byte
	var row2 Checking

	destAcct = toUint64(params["destAcct"])
	amount = toFloat32(params["amount"])

	goto BB6

BB6:
	// Combined table access: Checking (2 operations)
	keyBytes1, row1 = getChecking(tx, CheckingKey{custid: destAcct})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes1)
	c1_bal = row1.bal
	c1_custid = row1.Key.custid
	_tmp15 = c1_bal + amount
	c1_bal = _tmp15
	// Combined table access: Checking (2 operations)
	keyBytes2, row2 = getChecking(tx, CheckingKey{custid: destAcct})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes2)
	row2.bal = c1_bal
	row2.Key.custid = c1_custid
	putChecking(tx, CheckingKey{custid: destAcct}, row2)
	// return - no action
	// Flush caches for tables written in this hop
	flushCheckingCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// SendpaymentHop0Par calculates the partition for hop 0 without database access.
func SendpaymentHop0Par(params map[string]string) uint64 {
	var sendAcct uint64
	var amount float32
	var c0_custid uint64
	var c0_bal float32
	var _tmp13 float32

	var keyBytes1 []byte
	var row1 Checking
	var keyBytes2 []byte
	var row2 Checking

	sendAcct = toUint64(params["sendAcct"])
	amount = toFloat32(params["amount"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB5

BB5:
	// First table access (optimized group) - calculate partition: Checking
	if true { return getCheckingPar(CheckingKey{custid: sendAcct}) }
	// Combined table access: Checking (2 operations)
	keyBytes1, row1 = getChecking(tx, CheckingKey{custid: sendAcct})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes1)
	c0_bal = row1.bal
	c0_custid = row1.Key.custid
	_tmp13 = c0_bal - amount
	c0_bal = _tmp13
	// First table access (optimized group) - calculate partition: Checking
	if true { return putCheckingPar(CheckingKey{custid: sendAcct}) }
	// Combined table access: Checking (2 operations)
	keyBytes2, row2 = getChecking(tx, CheckingKey{custid: sendAcct})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes2)
	row2.bal = c0_bal
	row2.Key.custid = c0_custid
	putChecking(tx, CheckingKey{custid: sendAcct}, row2)
	panic("unexpected hop exit in partition")
}

// SendpaymentHop1Par calculates the partition for hop 1 without database access.
func SendpaymentHop1Par(params map[string]string) uint64 {
	var destAcct uint64
	var amount float32
	var c1_custid uint64
	var c1_bal float32
	var _tmp15 float32

	var keyBytes1 []byte
	var row1 Checking
	var keyBytes2 []byte
	var row2 Checking

	destAcct = toUint64(params["destAcct"])
	amount = toFloat32(params["amount"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB6

BB6:
	// First table access (optimized group) - calculate partition: Checking
	if true { return getCheckingPar(CheckingKey{custid: destAcct}) }
	// Combined table access: Checking (2 operations)
	keyBytes1, row1 = getChecking(tx, CheckingKey{custid: destAcct})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes1)
	c1_bal = row1.bal
	c1_custid = row1.Key.custid
	_tmp15 = c1_bal + amount
	c1_bal = _tmp15
	// First table access (optimized group) - calculate partition: Checking
	if true { return putCheckingPar(CheckingKey{custid: destAcct}) }
	// Combined table access: Checking (2 operations)
	keyBytes2, row2 = getChecking(tx, CheckingKey{custid: destAcct})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes2)
	row2.bal = c1_bal
	row2.Key.custid = c1_custid
	putChecking(tx, CheckingKey{custid: destAcct}, row2)
	return 0
}

func SendpaymentNextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(SendpaymentHop0Par(paramsCopy))
	case 1:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(SendpaymentHop1Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func SendpaymentChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 2)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    SendpaymentHop0,
		conflicts:  map[int32]int32{},
	}

	hops[1] = &Hop{
		id:         1,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    SendpaymentHop1,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: SendpaymentNextReq}
}

// legacy: use json package to avoid unused import error
func SendpaymentToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

