// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type AmalgamateChain struct {
	db   *bolt.DB
	hops []*Hop
}

// AmalgamateHop0 lowers hop 0 containing 1 basic block(s).
func AmalgamateHop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var custId0 uint64
	var s0_custid uint64
	var s0_bal float32
	var c0_custid uint64
	var c0_bal float32
	var total float32
	var _tmp2 float32

	var keyBytes1 []byte
	var row1 Savings
	var keyBytes2 []byte
	var row2 Checking
	var keyBytes3 []byte
	var row3 Savings
	var keyBytes4 []byte
	var row4 Checking

	custId0 = toUint64(params["custId0"])

	goto BB1

BB1:
	// Combined table access: Savings (2 operations)
	keyBytes1, row1 = getSavings(tx, SavingsKey{custid: custId0})
	rwSet = AddRWSet(rwSet, "Savings", keyBytes1)
	s0_bal = row1.bal
	s0_custid = row1.Key.custid
	// Combined table access: Checking (2 operations)
	keyBytes2, row2 = getChecking(tx, CheckingKey{custid: custId0})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes2)
	c0_bal = row2.bal
	c0_custid = row2.Key.custid
	_tmp2 = s0_bal + c0_bal
	total = _tmp2
	// Combined table access: Savings (2 operations)
	keyBytes3, row3 = getSavings(tx, SavingsKey{custid: custId0})
	rwSet = AddRWSet(rwSet, "Savings", keyBytes3)
	row3.bal = 0
	row3.Key.custid = s0_custid
	putSavings(tx, SavingsKey{custid: custId0}, row3)
	// Combined table access: Checking (2 operations)
	keyBytes4, row4 = getChecking(tx, CheckingKey{custid: custId0})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes4)
	row4.bal = 0
	row4.Key.custid = c0_custid
	putChecking(tx, CheckingKey{custid: custId0}, row4)
	// Flush caches for tables written in this hop
	flushSavingsCache(tx)
	flushCheckingCache(tx)
	// Write back variables to results
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		Results: []string{string(mustJSON(total))},
		RWSets: rwSet,
	}, nil
}

// AmalgamateHop1 lowers hop 1 containing 1 basic block(s).
func AmalgamateHop1(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var custId1 uint64
	var total float32
	var c1_custid uint64
	var c1_bal float32
	var _tmp4 float32

	var keyBytes1 []byte
	var row1 Checking
	var keyBytes2 []byte
	var row2 Checking

	custId1 = toUint64(params["custId1"])
	total = toFloat32(params["total"])

	goto BB2

BB2:
	// Combined table access: Checking (2 operations)
	keyBytes1, row1 = getChecking(tx, CheckingKey{custid: custId1})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes1)
	c1_bal = row1.bal
	c1_custid = row1.Key.custid
	_tmp4 = c1_bal + total
	c1_bal = _tmp4
	// Combined table access: Checking (2 operations)
	keyBytes2, row2 = getChecking(tx, CheckingKey{custid: custId1})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes2)
	row2.bal = c1_bal
	row2.Key.custid = c1_custid
	putChecking(tx, CheckingKey{custid: custId1}, row2)
	// return - no action
	// Flush caches for tables written in this hop
	flushCheckingCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// AmalgamateHop0Par calculates the partition for hop 0 without database access.
func AmalgamateHop0Par(params map[string]string) uint64 {
	var custId0 uint64
	var s0_custid uint64
	var s0_bal float32
	var c0_custid uint64
	var c0_bal float32
	var total float32
	var _tmp2 float32

	var keyBytes1 []byte
	var row1 Savings
	var keyBytes2 []byte
	var row2 Checking
	var keyBytes3 []byte
	var row3 Savings
	var keyBytes4 []byte
	var row4 Checking

	custId0 = toUint64(params["custId0"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB1

BB1:
	// First table access (optimized group) - calculate partition: Savings
	if true { return getSavingsPar(SavingsKey{custid: custId0}) }
	// Combined table access: Savings (2 operations)
	keyBytes1, row1 = getSavings(tx, SavingsKey{custid: custId0})
	rwSet = AddRWSet(rwSet, "Savings", keyBytes1)
	s0_bal = row1.bal
	s0_custid = row1.Key.custid
	// First table access (optimized group) - calculate partition: Checking
	if true { return getCheckingPar(CheckingKey{custid: custId0}) }
	// Combined table access: Checking (2 operations)
	keyBytes2, row2 = getChecking(tx, CheckingKey{custid: custId0})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes2)
	c0_bal = row2.bal
	c0_custid = row2.Key.custid
	_tmp2 = s0_bal + c0_bal
	total = _tmp2
	// First table access (optimized group) - calculate partition: Savings
	if true { return putSavingsPar(SavingsKey{custid: custId0}) }
	// Combined table access: Savings (2 operations)
	keyBytes3, row3 = getSavings(tx, SavingsKey{custid: custId0})
	rwSet = AddRWSet(rwSet, "Savings", keyBytes3)
	row3.bal = 0
	row3.Key.custid = s0_custid
	putSavings(tx, SavingsKey{custid: custId0}, row3)
	// First table access (optimized group) - calculate partition: Checking
	if true { return putCheckingPar(CheckingKey{custid: custId0}) }
	// Combined table access: Checking (2 operations)
	keyBytes4, row4 = getChecking(tx, CheckingKey{custid: custId0})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes4)
	row4.bal = 0
	row4.Key.custid = c0_custid
	putChecking(tx, CheckingKey{custid: custId0}, row4)
	// Unreachable: use variables in results
	_ = []string{string(mustJSON(total))}
	panic("unexpected hop exit in partition")
}

// AmalgamateHop1Par calculates the partition for hop 1 without database access.
func AmalgamateHop1Par(params map[string]string) uint64 {
	var custId1 uint64
	var total float32
	var c1_custid uint64
	var c1_bal float32
	var _tmp4 float32

	var keyBytes1 []byte
	var row1 Checking
	var keyBytes2 []byte
	var row2 Checking

	custId1 = toUint64(params["custId1"])
	total = toFloat32(params["total"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB2

BB2:
	// First table access (optimized group) - calculate partition: Checking
	if true { return getCheckingPar(CheckingKey{custid: custId1}) }
	// Combined table access: Checking (2 operations)
	keyBytes1, row1 = getChecking(tx, CheckingKey{custid: custId1})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes1)
	c1_bal = row1.bal
	c1_custid = row1.Key.custid
	_tmp4 = c1_bal + total
	c1_bal = _tmp4
	// First table access (optimized group) - calculate partition: Checking
	if true { return putCheckingPar(CheckingKey{custid: custId1}) }
	// Combined table access: Checking (2 operations)
	keyBytes2, row2 = getChecking(tx, CheckingKey{custid: custId1})
	rwSet = AddRWSet(rwSet, "Checking", keyBytes2)
	row2.bal = c1_bal
	row2.Key.custid = c1_custid
	putChecking(tx, CheckingKey{custid: custId1}, row2)
	return 0
}

func AmalgamateNextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(AmalgamateHop0Par(paramsCopy))
	case 1:
		// Extract variables from previous hop results
		params["total"] = in.Results[0]
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(AmalgamateHop1Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func AmalgamateChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 2)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    AmalgamateHop0,
		conflicts:  map[int32]int32{},
	}

	hops[1] = &Hop{
		id:         1,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    AmalgamateHop1,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: AmalgamateNextReq}
}

// legacy: use json package to avoid unused import error
func AmalgamateToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

