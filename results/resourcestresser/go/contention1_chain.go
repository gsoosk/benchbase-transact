// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type Contention1Chain struct {
	db   *bolt.DB
	hops []*Hop
}

// Contention1Hop0 lowers hop 0 containing 1 basic block(s).
func Contention1Hop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var key1 uint64
	var newSalary uint64
	var r1_empid uint64
	var r1_salary uint64

	var keyBytes1 []byte
	var row1 Locktable
	var keyBytes2 []byte
	var row2 Locktable

	key1 = toUint64(params["key1"])
	newSalary = toUint64(params["newSalary"])

	goto BB201

BB201:
	// TableGet: Locktable
	keyBytes1, row1 = getLocktable(tx, LocktableKey{empid: key1})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes1)
	r1_empid = row1.Key.empid
	r1_salary = newSalary
	// Combined table access: Locktable (2 operations)
	keyBytes2, row2 = getLocktable(tx, LocktableKey{empid: key1})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes2)
	row2.Key.empid = r1_empid
	row2.salary = r1_salary
	putLocktable(tx, LocktableKey{empid: key1}, row2)
	// Flush caches for tables written in this hop
	flushLocktableCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// Contention1Hop1 lowers hop 1 containing 1 basic block(s).
func Contention1Hop1(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var key2 uint64
	var newSalary uint64
	var r2_empid uint64
	var r2_salary uint64

	var keyBytes1 []byte
	var row1 Locktable
	var keyBytes2 []byte
	var row2 Locktable

	key2 = toUint64(params["key2"])
	newSalary = toUint64(params["newSalary"])

	goto BB202

BB202:
	// TableGet: Locktable
	keyBytes1, row1 = getLocktable(tx, LocktableKey{empid: key2})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes1)
	r2_empid = row1.Key.empid
	r2_salary = newSalary
	// Combined table access: Locktable (2 operations)
	keyBytes2, row2 = getLocktable(tx, LocktableKey{empid: key2})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes2)
	row2.Key.empid = r2_empid
	row2.salary = r2_salary
	putLocktable(tx, LocktableKey{empid: key2}, row2)
	// return - no action
	// Flush caches for tables written in this hop
	flushLocktableCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// Contention1Hop0Par calculates the partition for hop 0 without database access.
func Contention1Hop0Par(params map[string]string) uint64 {
	var key1 uint64
	var newSalary uint64
	var r1_empid uint64
	var r1_salary uint64

	var keyBytes1 []byte
	var row1 Locktable
	var keyBytes2 []byte
	var row2 Locktable

	key1 = toUint64(params["key1"])
	newSalary = toUint64(params["newSalary"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB201

BB201:
	// First table access - calculate partition: Locktable
	if true { return getLocktablePar(LocktableKey{empid: key1}) }
	// TableGet: Locktable
	keyBytes1, row1 = getLocktable(tx, LocktableKey{empid: key1})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes1)
	r1_empid = row1.Key.empid
	r1_salary = newSalary
	// First table access (optimized group) - calculate partition: Locktable
	if true { return putLocktablePar(LocktableKey{empid: key1}) }
	// Combined table access: Locktable (2 operations)
	keyBytes2, row2 = getLocktable(tx, LocktableKey{empid: key1})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes2)
	row2.Key.empid = r1_empid
	row2.salary = r1_salary
	putLocktable(tx, LocktableKey{empid: key1}, row2)
	panic("unexpected hop exit in partition")
}

// Contention1Hop1Par calculates the partition for hop 1 without database access.
func Contention1Hop1Par(params map[string]string) uint64 {
	var key2 uint64
	var newSalary uint64
	var r2_empid uint64
	var r2_salary uint64

	var keyBytes1 []byte
	var row1 Locktable
	var keyBytes2 []byte
	var row2 Locktable

	key2 = toUint64(params["key2"])
	newSalary = toUint64(params["newSalary"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB202

BB202:
	// First table access - calculate partition: Locktable
	if true { return getLocktablePar(LocktableKey{empid: key2}) }
	// TableGet: Locktable
	keyBytes1, row1 = getLocktable(tx, LocktableKey{empid: key2})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes1)
	r2_empid = row1.Key.empid
	r2_salary = newSalary
	// First table access (optimized group) - calculate partition: Locktable
	if true { return putLocktablePar(LocktableKey{empid: key2}) }
	// Combined table access: Locktable (2 operations)
	keyBytes2, row2 = getLocktable(tx, LocktableKey{empid: key2})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes2)
	row2.Key.empid = r2_empid
	row2.salary = r2_salary
	putLocktable(tx, LocktableKey{empid: key2}, row2)
	return 0
}

func Contention1NextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(Contention1Hop0Par(paramsCopy))
	case 1:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(Contention1Hop1Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func Contention1ChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 2)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    Contention1Hop0,
		conflicts:  map[int32]int32{},
	}

	hops[1] = &Hop{
		id:         1,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    Contention1Hop1,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: Contention1NextReq}
}

// legacy: use json package to avoid unused import error
func Contention1ToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

