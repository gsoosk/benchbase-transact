// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type Contention2Chain struct {
	db   *bolt.DB
	hops []*Hop
}

// Contention2Hop0 lowers hop 0 containing 1 basic block(s).
func Contention2Hop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var leftKey uint64
	var newSalary uint64
	var r1_empid uint64
	var r1_salary uint64

	var keyBytes1 []byte
	var row1 Locktable
	var keyBytes2 []byte
	var row2 Locktable

	leftKey = toUint64(params["leftKey"])
	newSalary = toUint64(params["newSalary"])

	goto BB203

BB203:
	// TableGet: Locktable
	keyBytes1, row1 = getLocktable(tx, LocktableKey{empid: leftKey})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes1)
	r1_empid = row1.Key.empid
	r1_salary = newSalary
	// Combined table access: Locktable (2 operations)
	keyBytes2, row2 = getLocktable(tx, LocktableKey{empid: leftKey})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes2)
	row2.Key.empid = r1_empid
	row2.salary = r1_salary
	putLocktable(tx, LocktableKey{empid: leftKey}, row2)
	// Flush caches for tables written in this hop
	flushLocktableCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// Contention2Hop1 lowers hop 1 containing 1 basic block(s).
func Contention2Hop1(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var leftKey uint64
	var newSalary uint64
	var r2_empid uint64
	var r2_salary uint64
	var _tmp203 uint64
	var _tmp205 uint64
	var _tmp206 uint64
	var _tmp207 uint64

	var keyBytes1 []byte
	var row1 Locktable
	var keyBytes2 []byte
	var row2 Locktable

	leftKey = toUint64(params["leftKey"])
	newSalary = toUint64(params["newSalary"])

	goto BB204

BB204:
	_tmp203 = leftKey + 1
	_tmp205 = _tmp203
	// TableGet: Locktable
	keyBytes1, row1 = getLocktable(tx, LocktableKey{empid: _tmp205})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes1)
	r2_empid = row1.Key.empid
	_tmp206 = _tmp205
	r2_salary = newSalary
	_tmp207 = _tmp206
	// Combined table access: Locktable (2 operations)
	keyBytes2, row2 = getLocktable(tx, LocktableKey{empid: _tmp207})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes2)
	row2.Key.empid = r2_empid
	row2.salary = r2_salary
	putLocktable(tx, LocktableKey{empid: _tmp207}, row2)
	// return - no action
	// Flush caches for tables written in this hop
	flushLocktableCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// Contention2Hop0Par calculates the partition for hop 0 without database access.
func Contention2Hop0Par(params map[string]string) uint64 {
	var leftKey uint64
	var newSalary uint64
	var r1_empid uint64
	var r1_salary uint64

	var keyBytes1 []byte
	var row1 Locktable
	var keyBytes2 []byte
	var row2 Locktable

	leftKey = toUint64(params["leftKey"])
	newSalary = toUint64(params["newSalary"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB203

BB203:
	// First table access - calculate partition: Locktable
	if true { return getLocktablePar(LocktableKey{empid: leftKey}) }
	// TableGet: Locktable
	keyBytes1, row1 = getLocktable(tx, LocktableKey{empid: leftKey})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes1)
	r1_empid = row1.Key.empid
	r1_salary = newSalary
	// First table access (optimized group) - calculate partition: Locktable
	if true { return putLocktablePar(LocktableKey{empid: leftKey}) }
	// Combined table access: Locktable (2 operations)
	keyBytes2, row2 = getLocktable(tx, LocktableKey{empid: leftKey})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes2)
	row2.Key.empid = r1_empid
	row2.salary = r1_salary
	putLocktable(tx, LocktableKey{empid: leftKey}, row2)
	panic("unexpected hop exit in partition")
}

// Contention2Hop1Par calculates the partition for hop 1 without database access.
func Contention2Hop1Par(params map[string]string) uint64 {
	var leftKey uint64
	var newSalary uint64
	var r2_empid uint64
	var r2_salary uint64
	var _tmp203 uint64
	var _tmp205 uint64
	var _tmp206 uint64
	var _tmp207 uint64

	var keyBytes1 []byte
	var row1 Locktable
	var keyBytes2 []byte
	var row2 Locktable

	leftKey = toUint64(params["leftKey"])
	newSalary = toUint64(params["newSalary"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB204

BB204:
	_tmp203 = leftKey + 1
	_tmp205 = _tmp203
	// First table access - calculate partition: Locktable
	if true { return getLocktablePar(LocktableKey{empid: _tmp205}) }
	// TableGet: Locktable
	keyBytes1, row1 = getLocktable(tx, LocktableKey{empid: _tmp205})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes1)
	r2_empid = row1.Key.empid
	_tmp206 = _tmp205
	r2_salary = newSalary
	_tmp207 = _tmp206
	// First table access (optimized group) - calculate partition: Locktable
	if true { return putLocktablePar(LocktableKey{empid: _tmp207}) }
	// Combined table access: Locktable (2 operations)
	keyBytes2, row2 = getLocktable(tx, LocktableKey{empid: _tmp207})
	rwSet = AddRWSet(rwSet, "Locktable", keyBytes2)
	row2.Key.empid = r2_empid
	row2.salary = r2_salary
	putLocktable(tx, LocktableKey{empid: _tmp207}, row2)
	return 0
}

func Contention2NextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(Contention2Hop0Par(paramsCopy))
	case 1:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(Contention2Hop1Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func Contention2ChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 2)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    Contention2Hop0,
		conflicts:  map[int32]int32{},
	}

	hops[1] = &Hop{
		id:         1,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    Contention2Hop1,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: Contention2NextReq}
}

// legacy: use json package to avoid unused import error
func Contention2ToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

