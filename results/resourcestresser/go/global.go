// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"sync"
	"github.com/boltdb/bolt"
)

func mustJSON(value any) []byte {
	bytes, err := json.Marshal(value)
	if err != nil {
		panic(err)
	}
	return bytes
}

// Global variable for the single-row write-back cache with a RWMutex.
var cputableCache struct {
	lock    sync.RWMutex
	row     *Cputable
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var locktableCache struct {
	lock    sync.RWMutex
	row     *Locktable
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var iotableCache struct {
	lock    sync.RWMutex
	row     *Iotable
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var iotablesmallrowCache struct {
	lock    sync.RWMutex
	row     *Iotablesmallrow
	isDirty bool
}

// FlushAllCaches flushes all table caches
func FlushAllCaches(tx *bolt.Tx) {
	flushCputableCache(tx)
	flushLocktableCache(tx)
	flushIotableCache(tx)
	flushIotablesmallrowCache(tx)
}

// Iterator getter functions for table primary keys
// Generated for each primary key field in each table

// get_empid_22 extracts empid from the iterator for table Cputable
func get_empid_22(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_empid_22: iterator is not valid")
	}

	// Deserialize the composite key to extract empid
	type CompositeKey struct {
		empid uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.empid
}

// get_empid_23 extracts empid from the iterator for table Locktable
func get_empid_23(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_empid_23: iterator is not valid")
	}

	// Deserialize the composite key to extract empid
	type CompositeKey struct {
		empid uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.empid
}

// get_empid_24 extracts empid from the iterator for table Iotable
func get_empid_24(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_empid_24: iterator is not valid")
	}

	// Deserialize the composite key to extract empid
	type CompositeKey struct {
		empid uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.empid
}

// get_empid_25 extracts empid from the iterator for table Iotablesmallrow
func get_empid_25(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_empid_25: iterator is not valid")
	}

	// Deserialize the composite key to extract empid
	type CompositeKey struct {
		empid uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.empid
}

const (
	__slice__ uint64 = 0
	__shards__ uint64 = 1
	CPU_RANGE uint64 = 100
)

type CputableKey struct {
	empid uint64
}

type Cputable struct {
	Key CputableKey
	passwd string
}

// flushCputableCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushCputableCacheUnlocked(tx *bolt.Tx) {
	if cputableCache.row != nil && cputableCache.isDirty {
		bucket := tx.Bucket([]byte("tCputable"))
		if bucket == nil {
			panic("missing bucket tCputable")
		}
		var key CputableKey = cputableCache.row.Key
		var row Cputable = *cputableCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	cputableCache.row = nil
	cputableCache.isDirty = false
}

// flushCputableCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushCputableCache(tx *bolt.Tx) {
	cputableCache.lock.Lock()
	defer cputableCache.lock.Unlock()
	flushCputableCacheUnlocked(tx)
}

// getCputable fetches data, utilizing the cache in a highly concurrent and safe manner.
func getCputable(tx *bolt.Tx, key CputableKey) ([]byte, Cputable) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	cputableCache.lock.RLock()
	if cputableCache.row != nil && cputableCache.row.Key == key {
		row := *cputableCache.row
		cputableCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	cputableCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	cputableCache.lock.Lock()
	defer cputableCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if cputableCache.row != nil && cputableCache.row.Key == key {
		return mustJSON(key), *cputableCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushCputableCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tCputable"))
	if bucket == nil {
		panic("missing bucket tCputable")
	}
	keyBytes := mustJSON(key)
	var row Cputable
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	cputableCache.row = &row
	cputableCache.isDirty = false

	return keyBytes, row
}

// putCputable updates the cache and marks it as dirty (write-back).
func putCputable(tx *bolt.Tx, key CputableKey, row Cputable) {
	cputableCache.lock.Lock()
	defer cputableCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if cputableCache.row != nil && cputableCache.row.Key != key {
		flushCputableCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	cputableCache.row = &row
	cputableCache.isDirty = true
}

func getCputablePar(key CputableKey) uint64 {
	// Call partition function to determine shard
	return f(key.empid)
}

func putCputablePar(key CputableKey) uint64 {
	// Call partition function to determine shard
	return f(key.empid)
}

type LocktableKey struct {
	empid uint64
}

type Locktable struct {
	Key LocktableKey
	salary uint64
}

// flushLocktableCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushLocktableCacheUnlocked(tx *bolt.Tx) {
	if locktableCache.row != nil && locktableCache.isDirty {
		bucket := tx.Bucket([]byte("tLocktable"))
		if bucket == nil {
			panic("missing bucket tLocktable")
		}
		var key LocktableKey = locktableCache.row.Key
		var row Locktable = *locktableCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	locktableCache.row = nil
	locktableCache.isDirty = false
}

// flushLocktableCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushLocktableCache(tx *bolt.Tx) {
	locktableCache.lock.Lock()
	defer locktableCache.lock.Unlock()
	flushLocktableCacheUnlocked(tx)
}

// getLocktable fetches data, utilizing the cache in a highly concurrent and safe manner.
func getLocktable(tx *bolt.Tx, key LocktableKey) ([]byte, Locktable) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	locktableCache.lock.RLock()
	if locktableCache.row != nil && locktableCache.row.Key == key {
		row := *locktableCache.row
		locktableCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	locktableCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	locktableCache.lock.Lock()
	defer locktableCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if locktableCache.row != nil && locktableCache.row.Key == key {
		return mustJSON(key), *locktableCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushLocktableCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tLocktable"))
	if bucket == nil {
		panic("missing bucket tLocktable")
	}
	keyBytes := mustJSON(key)
	var row Locktable
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	locktableCache.row = &row
	locktableCache.isDirty = false

	return keyBytes, row
}

// putLocktable updates the cache and marks it as dirty (write-back).
func putLocktable(tx *bolt.Tx, key LocktableKey, row Locktable) {
	locktableCache.lock.Lock()
	defer locktableCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if locktableCache.row != nil && locktableCache.row.Key != key {
		flushLocktableCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	locktableCache.row = &row
	locktableCache.isDirty = true
}

func getLocktablePar(key LocktableKey) uint64 {
	// Call partition function to determine shard
	return f(key.empid)
}

func putLocktablePar(key LocktableKey) uint64 {
	// Call partition function to determine shard
	return f(key.empid)
}

type IotableKey struct {
	empid uint64
}

type Iotable struct {
	Key IotableKey
	data1 string
	data2 string
}

// flushIotableCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushIotableCacheUnlocked(tx *bolt.Tx) {
	if iotableCache.row != nil && iotableCache.isDirty {
		bucket := tx.Bucket([]byte("tIotable"))
		if bucket == nil {
			panic("missing bucket tIotable")
		}
		var key IotableKey = iotableCache.row.Key
		var row Iotable = *iotableCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	iotableCache.row = nil
	iotableCache.isDirty = false
}

// flushIotableCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushIotableCache(tx *bolt.Tx) {
	iotableCache.lock.Lock()
	defer iotableCache.lock.Unlock()
	flushIotableCacheUnlocked(tx)
}

// getIotable fetches data, utilizing the cache in a highly concurrent and safe manner.
func getIotable(tx *bolt.Tx, key IotableKey) ([]byte, Iotable) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	iotableCache.lock.RLock()
	if iotableCache.row != nil && iotableCache.row.Key == key {
		row := *iotableCache.row
		iotableCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	iotableCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	iotableCache.lock.Lock()
	defer iotableCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if iotableCache.row != nil && iotableCache.row.Key == key {
		return mustJSON(key), *iotableCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushIotableCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tIotable"))
	if bucket == nil {
		panic("missing bucket tIotable")
	}
	keyBytes := mustJSON(key)
	var row Iotable
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	iotableCache.row = &row
	iotableCache.isDirty = false

	return keyBytes, row
}

// putIotable updates the cache and marks it as dirty (write-back).
func putIotable(tx *bolt.Tx, key IotableKey, row Iotable) {
	iotableCache.lock.Lock()
	defer iotableCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if iotableCache.row != nil && iotableCache.row.Key != key {
		flushIotableCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	iotableCache.row = &row
	iotableCache.isDirty = true
}

func getIotablePar(key IotableKey) uint64 {
	// Call partition function to determine shard
	return f(key.empid)
}

func putIotablePar(key IotableKey) uint64 {
	// Call partition function to determine shard
	return f(key.empid)
}

type IotablesmallrowKey struct {
	empid uint64
}

type Iotablesmallrow struct {
	Key IotablesmallrowKey
	flag1 uint64
}

// flushIotablesmallrowCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushIotablesmallrowCacheUnlocked(tx *bolt.Tx) {
	if iotablesmallrowCache.row != nil && iotablesmallrowCache.isDirty {
		bucket := tx.Bucket([]byte("tIotablesmallrow"))
		if bucket == nil {
			panic("missing bucket tIotablesmallrow")
		}
		var key IotablesmallrowKey = iotablesmallrowCache.row.Key
		var row Iotablesmallrow = *iotablesmallrowCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	iotablesmallrowCache.row = nil
	iotablesmallrowCache.isDirty = false
}

// flushIotablesmallrowCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushIotablesmallrowCache(tx *bolt.Tx) {
	iotablesmallrowCache.lock.Lock()
	defer iotablesmallrowCache.lock.Unlock()
	flushIotablesmallrowCacheUnlocked(tx)
}

// getIotablesmallrow fetches data, utilizing the cache in a highly concurrent and safe manner.
func getIotablesmallrow(tx *bolt.Tx, key IotablesmallrowKey) ([]byte, Iotablesmallrow) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	iotablesmallrowCache.lock.RLock()
	if iotablesmallrowCache.row != nil && iotablesmallrowCache.row.Key == key {
		row := *iotablesmallrowCache.row
		iotablesmallrowCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	iotablesmallrowCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	iotablesmallrowCache.lock.Lock()
	defer iotablesmallrowCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if iotablesmallrowCache.row != nil && iotablesmallrowCache.row.Key == key {
		return mustJSON(key), *iotablesmallrowCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushIotablesmallrowCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tIotablesmallrow"))
	if bucket == nil {
		panic("missing bucket tIotablesmallrow")
	}
	keyBytes := mustJSON(key)
	var row Iotablesmallrow
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	iotablesmallrowCache.row = &row
	iotablesmallrowCache.isDirty = false

	return keyBytes, row
}

// putIotablesmallrow updates the cache and marks it as dirty (write-back).
func putIotablesmallrow(tx *bolt.Tx, key IotablesmallrowKey, row Iotablesmallrow) {
	iotablesmallrowCache.lock.Lock()
	defer iotablesmallrowCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if iotablesmallrowCache.row != nil && iotablesmallrowCache.row.Key != key {
		flushIotablesmallrowCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	iotablesmallrowCache.row = &row
	iotablesmallrowCache.isDirty = true
}

func getIotablesmallrowPar(key IotablesmallrowKey) uint64 {
	// Call partition function to determine shard
	return f(key.empid)
}

func putIotablesmallrowPar(key IotablesmallrowKey) uint64 {
	// Call partition function to determine shard
	return f(key.empid)
}

func f(x uint64) uint64 {
	goto BB0

BB0:
	return x
}

func Scheds() {
	// Initialize scheduler maps
	LockSchedulers = make(map[string]*Scheduler)
	AccessSchedulers = make(map[string]*Scheduler)
	AccessDSchedulers = make(map[string]*Scheduler)

	// Create one lock scheduler for each table
	LockSchedulers["Cputable"] = NewScheduler()
	LockSchedulers["Locktable"] = NewScheduler()
	LockSchedulers["Iotable"] = NewScheduler()
	LockSchedulers["Iotablesmallrow"] = NewScheduler()

}

func Chains(db *bolt.DB) []*Chain {
	return []*Chain{
		Cpu1ChainImpl(db),
		Cpu2ChainImpl(db),
		Contention1ChainImpl(db),
		Contention2ChainImpl(db),
		Io1ChainImpl(db),
		Io2ChainImpl(db),
	}
}

