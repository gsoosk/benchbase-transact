// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"sync"
	"github.com/boltdb/bolt"
)

func mustJSON(value any) []byte {
	bytes, err := json.Marshal(value)
	if err != nil {
		panic(err)
	}
	return bytes
}

// Global variable for the single-row write-back cache with a RWMutex.
var useracctCache struct {
	lock    sync.RWMutex
	row     *Useracct
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var regionCache struct {
	lock    sync.RWMutex
	row     *Region
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var itemCache struct {
	lock    sync.RWMutex
	row     *Item
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var item_bidCache struct {
	lock    sync.RWMutex
	row     *ItemBid
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var item_max_bidCache struct {
	lock    sync.RWMutex
	row     *ItemMaxBid
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var item_commentCache struct {
	lock    sync.RWMutex
	row     *ItemComment
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var useracct_itemCache struct {
	lock    sync.RWMutex
	row     *UseracctItem
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var useracct_feedbackCache struct {
	lock    sync.RWMutex
	row     *UseracctFeedback
	isDirty bool
}

// Global variable for the single-row write-back cache with a RWMutex.
var useracct_watchCache struct {
	lock    sync.RWMutex
	row     *UseracctWatch
	isDirty bool
}

// FlushAllCaches flushes all table caches
func FlushAllCaches(tx *bolt.Tx) {
	flushUseracctCache(tx)
	flushRegionCache(tx)
	flushItemCache(tx)
	flushItemBidCache(tx)
	flushItemMaxBidCache(tx)
	flushItemCommentCache(tx)
	flushUseracctItemCache(tx)
	flushUseracctFeedbackCache(tx)
	flushUseracctWatchCache(tx)
}

// Iterator getter functions for table primary keys
// Generated for each primary key field in each table

// get_u_id_28 extracts u_id from the iterator for table Useracct
func get_u_id_28(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_u_id_28: iterator is not valid")
	}

	// Deserialize the composite key to extract u_id
	type CompositeKey struct {
		u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.u_id
}

// get_r_id_29 extracts r_id from the iterator for table Region
func get_r_id_29(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_r_id_29: iterator is not valid")
	}

	// Deserialize the composite key to extract r_id
	type CompositeKey struct {
		r_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.r_id
}

// get_i_id_30 extracts i_id from the iterator for table Item
func get_i_id_30(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_i_id_30: iterator is not valid")
	}

	// Deserialize the composite key to extract i_id
	type CompositeKey struct {
		i_id uint64
		i_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.i_id
}

// get_i_u_id_31 extracts i_u_id from the iterator for table Item
func get_i_u_id_31(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_i_u_id_31: iterator is not valid")
	}

	// Deserialize the composite key to extract i_u_id
	type CompositeKey struct {
		i_id uint64
		i_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.i_u_id
}

// get_ib_id_32 extracts ib_id from the iterator for table ItemBid
func get_ib_id_32(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ib_id_32: iterator is not valid")
	}

	// Deserialize the composite key to extract ib_id
	type CompositeKey struct {
		ib_id uint64
		ib_i_id uint64
		ib_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ib_id
}

// get_ib_i_id_33 extracts ib_i_id from the iterator for table ItemBid
func get_ib_i_id_33(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ib_i_id_33: iterator is not valid")
	}

	// Deserialize the composite key to extract ib_i_id
	type CompositeKey struct {
		ib_id uint64
		ib_i_id uint64
		ib_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ib_i_id
}

// get_ib_u_id_34 extracts ib_u_id from the iterator for table ItemBid
func get_ib_u_id_34(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ib_u_id_34: iterator is not valid")
	}

	// Deserialize the composite key to extract ib_u_id
	type CompositeKey struct {
		ib_id uint64
		ib_i_id uint64
		ib_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ib_u_id
}

// get_imb_i_id_35 extracts imb_i_id from the iterator for table ItemMaxBid
func get_imb_i_id_35(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_imb_i_id_35: iterator is not valid")
	}

	// Deserialize the composite key to extract imb_i_id
	type CompositeKey struct {
		imb_i_id uint64
		imb_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.imb_i_id
}

// get_imb_u_id_36 extracts imb_u_id from the iterator for table ItemMaxBid
func get_imb_u_id_36(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_imb_u_id_36: iterator is not valid")
	}

	// Deserialize the composite key to extract imb_u_id
	type CompositeKey struct {
		imb_i_id uint64
		imb_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.imb_u_id
}

// get_ic_id_37 extracts ic_id from the iterator for table ItemComment
func get_ic_id_37(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ic_id_37: iterator is not valid")
	}

	// Deserialize the composite key to extract ic_id
	type CompositeKey struct {
		ic_id uint64
		ic_i_id uint64
		ic_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ic_id
}

// get_ic_i_id_38 extracts ic_i_id from the iterator for table ItemComment
func get_ic_i_id_38(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ic_i_id_38: iterator is not valid")
	}

	// Deserialize the composite key to extract ic_i_id
	type CompositeKey struct {
		ic_id uint64
		ic_i_id uint64
		ic_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ic_i_id
}

// get_ic_u_id_39 extracts ic_u_id from the iterator for table ItemComment
func get_ic_u_id_39(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ic_u_id_39: iterator is not valid")
	}

	// Deserialize the composite key to extract ic_u_id
	type CompositeKey struct {
		ic_id uint64
		ic_i_id uint64
		ic_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ic_u_id
}

// get_ui_u_id_40 extracts ui_u_id from the iterator for table UseracctItem
func get_ui_u_id_40(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ui_u_id_40: iterator is not valid")
	}

	// Deserialize the composite key to extract ui_u_id
	type CompositeKey struct {
		ui_u_id uint64
		ui_i_id uint64
		ui_i_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ui_u_id
}

// get_ui_i_id_41 extracts ui_i_id from the iterator for table UseracctItem
func get_ui_i_id_41(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ui_i_id_41: iterator is not valid")
	}

	// Deserialize the composite key to extract ui_i_id
	type CompositeKey struct {
		ui_u_id uint64
		ui_i_id uint64
		ui_i_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ui_i_id
}

// get_ui_i_u_id_42 extracts ui_i_u_id from the iterator for table UseracctItem
func get_ui_i_u_id_42(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_ui_i_u_id_42: iterator is not valid")
	}

	// Deserialize the composite key to extract ui_i_u_id
	type CompositeKey struct {
		ui_u_id uint64
		ui_i_id uint64
		ui_i_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.ui_i_u_id
}

// get_uf_u_id_43 extracts uf_u_id from the iterator for table UseracctFeedback
func get_uf_u_id_43(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uf_u_id_43: iterator is not valid")
	}

	// Deserialize the composite key to extract uf_u_id
	type CompositeKey struct {
		uf_u_id uint64
		uf_i_id uint64
		uf_i_u_id uint64
		uf_from_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uf_u_id
}

// get_uf_i_id_44 extracts uf_i_id from the iterator for table UseracctFeedback
func get_uf_i_id_44(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uf_i_id_44: iterator is not valid")
	}

	// Deserialize the composite key to extract uf_i_id
	type CompositeKey struct {
		uf_u_id uint64
		uf_i_id uint64
		uf_i_u_id uint64
		uf_from_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uf_i_id
}

// get_uf_i_u_id_45 extracts uf_i_u_id from the iterator for table UseracctFeedback
func get_uf_i_u_id_45(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uf_i_u_id_45: iterator is not valid")
	}

	// Deserialize the composite key to extract uf_i_u_id
	type CompositeKey struct {
		uf_u_id uint64
		uf_i_id uint64
		uf_i_u_id uint64
		uf_from_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uf_i_u_id
}

// get_uf_from_id_46 extracts uf_from_id from the iterator for table UseracctFeedback
func get_uf_from_id_46(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uf_from_id_46: iterator is not valid")
	}

	// Deserialize the composite key to extract uf_from_id
	type CompositeKey struct {
		uf_u_id uint64
		uf_i_id uint64
		uf_i_u_id uint64
		uf_from_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uf_from_id
}

// get_uw_u_id_47 extracts uw_u_id from the iterator for table UseracctWatch
func get_uw_u_id_47(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uw_u_id_47: iterator is not valid")
	}

	// Deserialize the composite key to extract uw_u_id
	type CompositeKey struct {
		uw_u_id uint64
		uw_i_id uint64
		uw_i_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uw_u_id
}

// get_uw_i_id_48 extracts uw_i_id from the iterator for table UseracctWatch
func get_uw_i_id_48(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uw_i_id_48: iterator is not valid")
	}

	// Deserialize the composite key to extract uw_i_id
	type CompositeKey struct {
		uw_u_id uint64
		uw_i_id uint64
		uw_i_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uw_i_id
}

// get_uw_i_u_id_49 extracts uw_i_u_id from the iterator for table UseracctWatch
func get_uw_i_u_id_49(iter *Iterator) uint64 {
	if !iter.isValid {
		panic("get_uw_i_u_id_49: iterator is not valid")
	}

	// Deserialize the composite key to extract uw_i_u_id
	type CompositeKey struct {
		uw_u_id uint64
		uw_i_id uint64
		uw_i_u_id uint64
	}

	var key CompositeKey
	if err := json.Unmarshal(iter.currentKey, &key); err != nil {
		panic(err)
	}

	return key.uw_i_u_id
}

const (
	__slice__ uint64 = 0
	__shards__ uint64 = 1
	ITEM_STATUS_OPEN uint64 = 0
	ITEM_STATUS_WAITING_FOR_PURCHASE uint64 = 1
	ITEM_STATUS_CLOSED uint64 = 2
)

type UseracctKey struct {
	u_id uint64
}

type Useracct struct {
	Key UseracctKey
	u_rating uint64
	u_balance float32
	u_comments uint64
	u_r_id uint64
	u_sattr0 string
	u_sattr1 string
	u_sattr2 string
	u_sattr3 string
	u_sattr4 string
}

// flushUseracctCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushUseracctCacheUnlocked(tx *bolt.Tx) {
	if useracctCache.row != nil && useracctCache.isDirty {
		bucket := tx.Bucket([]byte("tUseracct"))
		if bucket == nil {
			panic("missing bucket tUseracct")
		}
		var key UseracctKey = useracctCache.row.Key
		var row Useracct = *useracctCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	useracctCache.row = nil
	useracctCache.isDirty = false
}

// flushUseracctCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushUseracctCache(tx *bolt.Tx) {
	useracctCache.lock.Lock()
	defer useracctCache.lock.Unlock()
	flushUseracctCacheUnlocked(tx)
}

// getUseracct fetches data, utilizing the cache in a highly concurrent and safe manner.
func getUseracct(tx *bolt.Tx, key UseracctKey) ([]byte, Useracct) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	useracctCache.lock.RLock()
	if useracctCache.row != nil && useracctCache.row.Key == key {
		row := *useracctCache.row
		useracctCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	useracctCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	useracctCache.lock.Lock()
	defer useracctCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if useracctCache.row != nil && useracctCache.row.Key == key {
		return mustJSON(key), *useracctCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushUseracctCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tUseracct"))
	if bucket == nil {
		panic("missing bucket tUseracct")
	}
	keyBytes := mustJSON(key)
	var row Useracct
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	useracctCache.row = &row
	useracctCache.isDirty = false

	return keyBytes, row
}

// putUseracct updates the cache and marks it as dirty (write-back).
func putUseracct(tx *bolt.Tx, key UseracctKey, row Useracct) {
	useracctCache.lock.Lock()
	defer useracctCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if useracctCache.row != nil && useracctCache.row.Key != key {
		flushUseracctCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	useracctCache.row = &row
	useracctCache.isDirty = true
}

func getUseracctPar(key UseracctKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.u_id)
}

func putUseracctPar(key UseracctKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.u_id)
}

type RegionKey struct {
	r_id uint64
}

type Region struct {
	Key RegionKey
	r_name string
}

// flushRegionCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushRegionCacheUnlocked(tx *bolt.Tx) {
	if regionCache.row != nil && regionCache.isDirty {
		bucket := tx.Bucket([]byte("tRegion"))
		if bucket == nil {
			panic("missing bucket tRegion")
		}
		var key RegionKey = regionCache.row.Key
		var row Region = *regionCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	regionCache.row = nil
	regionCache.isDirty = false
}

// flushRegionCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushRegionCache(tx *bolt.Tx) {
	regionCache.lock.Lock()
	defer regionCache.lock.Unlock()
	flushRegionCacheUnlocked(tx)
}

// getRegion fetches data, utilizing the cache in a highly concurrent and safe manner.
func getRegion(tx *bolt.Tx, key RegionKey) ([]byte, Region) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	regionCache.lock.RLock()
	if regionCache.row != nil && regionCache.row.Key == key {
		row := *regionCache.row
		regionCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	regionCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	regionCache.lock.Lock()
	defer regionCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if regionCache.row != nil && regionCache.row.Key == key {
		return mustJSON(key), *regionCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushRegionCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tRegion"))
	if bucket == nil {
		panic("missing bucket tRegion")
	}
	keyBytes := mustJSON(key)
	var row Region
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	regionCache.row = &row
	regionCache.isDirty = false

	return keyBytes, row
}

// putRegion updates the cache and marks it as dirty (write-back).
func putRegion(tx *bolt.Tx, key RegionKey, row Region) {
	regionCache.lock.Lock()
	defer regionCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if regionCache.row != nil && regionCache.row.Key != key {
		flushRegionCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	regionCache.row = &row
	regionCache.isDirty = true
}

func getRegionPar(key RegionKey) uint64 {
	// Call partition function to determine shard
	return p_region(key.r_id)
}

func putRegionPar(key RegionKey) uint64 {
	// Call partition function to determine shard
	return p_region(key.r_id)
}

type ItemKey struct {
	i_id uint64
	i_u_id uint64
}

type Item struct {
	Key ItemKey
	i_name string
	i_initial_price float32
	i_current_price float32
	i_num_bids uint64
	i_end_date uint64
	i_start_date uint64
	i_status uint64
	i_num_comments uint64
	i_c_id uint64
}

// flushItemCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushItemCacheUnlocked(tx *bolt.Tx) {
	if itemCache.row != nil && itemCache.isDirty {
		bucket := tx.Bucket([]byte("tItem"))
		if bucket == nil {
			panic("missing bucket tItem")
		}
		var key ItemKey = itemCache.row.Key
		var row Item = *itemCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	itemCache.row = nil
	itemCache.isDirty = false
}

// flushItemCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushItemCache(tx *bolt.Tx) {
	itemCache.lock.Lock()
	defer itemCache.lock.Unlock()
	flushItemCacheUnlocked(tx)
}

// getItem fetches data, utilizing the cache in a highly concurrent and safe manner.
func getItem(tx *bolt.Tx, key ItemKey) ([]byte, Item) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	itemCache.lock.RLock()
	if itemCache.row != nil && itemCache.row.Key == key {
		row := *itemCache.row
		itemCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	itemCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	itemCache.lock.Lock()
	defer itemCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if itemCache.row != nil && itemCache.row.Key == key {
		return mustJSON(key), *itemCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushItemCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tItem"))
	if bucket == nil {
		panic("missing bucket tItem")
	}
	keyBytes := mustJSON(key)
	var row Item
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	itemCache.row = &row
	itemCache.isDirty = false

	return keyBytes, row
}

// putItem updates the cache and marks it as dirty (write-back).
func putItem(tx *bolt.Tx, key ItemKey, row Item) {
	itemCache.lock.Lock()
	defer itemCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if itemCache.row != nil && itemCache.row.Key != key {
		flushItemCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	itemCache.row = &row
	itemCache.isDirty = true
}

func getItemPar(key ItemKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.i_u_id)
}

func putItemPar(key ItemKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.i_u_id)
}

type ItemBidKey struct {
	ib_id uint64
	ib_i_id uint64
	ib_u_id uint64
}

type ItemBid struct {
	Key ItemBidKey
	ib_buyer_id uint64
	ib_bid float32
	ib_max_bid float32
}

// flushItemBidCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushItemBidCacheUnlocked(tx *bolt.Tx) {
	if item_bidCache.row != nil && item_bidCache.isDirty {
		bucket := tx.Bucket([]byte("tItemBid"))
		if bucket == nil {
			panic("missing bucket tItemBid")
		}
		var key ItemBidKey = item_bidCache.row.Key
		var row ItemBid = *item_bidCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	item_bidCache.row = nil
	item_bidCache.isDirty = false
}

// flushItemBidCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushItemBidCache(tx *bolt.Tx) {
	item_bidCache.lock.Lock()
	defer item_bidCache.lock.Unlock()
	flushItemBidCacheUnlocked(tx)
}

// getItemBid fetches data, utilizing the cache in a highly concurrent and safe manner.
func getItemBid(tx *bolt.Tx, key ItemBidKey) ([]byte, ItemBid) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	item_bidCache.lock.RLock()
	if item_bidCache.row != nil && item_bidCache.row.Key == key {
		row := *item_bidCache.row
		item_bidCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	item_bidCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	item_bidCache.lock.Lock()
	defer item_bidCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if item_bidCache.row != nil && item_bidCache.row.Key == key {
		return mustJSON(key), *item_bidCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushItemBidCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tItemBid"))
	if bucket == nil {
		panic("missing bucket tItemBid")
	}
	keyBytes := mustJSON(key)
	var row ItemBid
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	item_bidCache.row = &row
	item_bidCache.isDirty = false

	return keyBytes, row
}

// putItemBid updates the cache and marks it as dirty (write-back).
func putItemBid(tx *bolt.Tx, key ItemBidKey, row ItemBid) {
	item_bidCache.lock.Lock()
	defer item_bidCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if item_bidCache.row != nil && item_bidCache.row.Key != key {
		flushItemBidCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	item_bidCache.row = &row
	item_bidCache.isDirty = true
}

func getItemBidPar(key ItemBidKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.ib_u_id)
}

func putItemBidPar(key ItemBidKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.ib_u_id)
}

type ItemMaxBidKey struct {
	imb_i_id uint64
	imb_u_id uint64
}

type ItemMaxBid struct {
	Key ItemMaxBidKey
	imb_ib_id uint64
	imb_ib_i_id uint64
	imb_ib_u_id uint64
}

// flushItemMaxBidCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushItemMaxBidCacheUnlocked(tx *bolt.Tx) {
	if item_max_bidCache.row != nil && item_max_bidCache.isDirty {
		bucket := tx.Bucket([]byte("tItemMaxBid"))
		if bucket == nil {
			panic("missing bucket tItemMaxBid")
		}
		var key ItemMaxBidKey = item_max_bidCache.row.Key
		var row ItemMaxBid = *item_max_bidCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	item_max_bidCache.row = nil
	item_max_bidCache.isDirty = false
}

// flushItemMaxBidCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushItemMaxBidCache(tx *bolt.Tx) {
	item_max_bidCache.lock.Lock()
	defer item_max_bidCache.lock.Unlock()
	flushItemMaxBidCacheUnlocked(tx)
}

// getItemMaxBid fetches data, utilizing the cache in a highly concurrent and safe manner.
func getItemMaxBid(tx *bolt.Tx, key ItemMaxBidKey) ([]byte, ItemMaxBid) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	item_max_bidCache.lock.RLock()
	if item_max_bidCache.row != nil && item_max_bidCache.row.Key == key {
		row := *item_max_bidCache.row
		item_max_bidCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	item_max_bidCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	item_max_bidCache.lock.Lock()
	defer item_max_bidCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if item_max_bidCache.row != nil && item_max_bidCache.row.Key == key {
		return mustJSON(key), *item_max_bidCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushItemMaxBidCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tItemMaxBid"))
	if bucket == nil {
		panic("missing bucket tItemMaxBid")
	}
	keyBytes := mustJSON(key)
	var row ItemMaxBid
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	item_max_bidCache.row = &row
	item_max_bidCache.isDirty = false

	return keyBytes, row
}

// putItemMaxBid updates the cache and marks it as dirty (write-back).
func putItemMaxBid(tx *bolt.Tx, key ItemMaxBidKey, row ItemMaxBid) {
	item_max_bidCache.lock.Lock()
	defer item_max_bidCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if item_max_bidCache.row != nil && item_max_bidCache.row.Key != key {
		flushItemMaxBidCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	item_max_bidCache.row = &row
	item_max_bidCache.isDirty = true
}

func getItemMaxBidPar(key ItemMaxBidKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.imb_u_id)
}

func putItemMaxBidPar(key ItemMaxBidKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.imb_u_id)
}

type ItemCommentKey struct {
	ic_id uint64
	ic_i_id uint64
	ic_u_id uint64
}

type ItemComment struct {
	Key ItemCommentKey
	ic_buyer_id uint64
	ic_question string
	ic_response string
}

// flushItemCommentCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushItemCommentCacheUnlocked(tx *bolt.Tx) {
	if item_commentCache.row != nil && item_commentCache.isDirty {
		bucket := tx.Bucket([]byte("tItemComment"))
		if bucket == nil {
			panic("missing bucket tItemComment")
		}
		var key ItemCommentKey = item_commentCache.row.Key
		var row ItemComment = *item_commentCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	item_commentCache.row = nil
	item_commentCache.isDirty = false
}

// flushItemCommentCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushItemCommentCache(tx *bolt.Tx) {
	item_commentCache.lock.Lock()
	defer item_commentCache.lock.Unlock()
	flushItemCommentCacheUnlocked(tx)
}

// getItemComment fetches data, utilizing the cache in a highly concurrent and safe manner.
func getItemComment(tx *bolt.Tx, key ItemCommentKey) ([]byte, ItemComment) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	item_commentCache.lock.RLock()
	if item_commentCache.row != nil && item_commentCache.row.Key == key {
		row := *item_commentCache.row
		item_commentCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	item_commentCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	item_commentCache.lock.Lock()
	defer item_commentCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if item_commentCache.row != nil && item_commentCache.row.Key == key {
		return mustJSON(key), *item_commentCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushItemCommentCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tItemComment"))
	if bucket == nil {
		panic("missing bucket tItemComment")
	}
	keyBytes := mustJSON(key)
	var row ItemComment
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	item_commentCache.row = &row
	item_commentCache.isDirty = false

	return keyBytes, row
}

// putItemComment updates the cache and marks it as dirty (write-back).
func putItemComment(tx *bolt.Tx, key ItemCommentKey, row ItemComment) {
	item_commentCache.lock.Lock()
	defer item_commentCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if item_commentCache.row != nil && item_commentCache.row.Key != key {
		flushItemCommentCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	item_commentCache.row = &row
	item_commentCache.isDirty = true
}

func getItemCommentPar(key ItemCommentKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.ic_u_id)
}

func putItemCommentPar(key ItemCommentKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.ic_u_id)
}

type UseracctItemKey struct {
	ui_u_id uint64
	ui_i_id uint64
	ui_i_u_id uint64
}

type UseracctItem struct {
	Key UseracctItemKey
}

// flushUseracctItemCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushUseracctItemCacheUnlocked(tx *bolt.Tx) {
	if useracct_itemCache.row != nil && useracct_itemCache.isDirty {
		bucket := tx.Bucket([]byte("tUseracctItem"))
		if bucket == nil {
			panic("missing bucket tUseracctItem")
		}
		var key UseracctItemKey = useracct_itemCache.row.Key
		var row UseracctItem = *useracct_itemCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	useracct_itemCache.row = nil
	useracct_itemCache.isDirty = false
}

// flushUseracctItemCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushUseracctItemCache(tx *bolt.Tx) {
	useracct_itemCache.lock.Lock()
	defer useracct_itemCache.lock.Unlock()
	flushUseracctItemCacheUnlocked(tx)
}

// getUseracctItem fetches data, utilizing the cache in a highly concurrent and safe manner.
func getUseracctItem(tx *bolt.Tx, key UseracctItemKey) ([]byte, UseracctItem) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	useracct_itemCache.lock.RLock()
	if useracct_itemCache.row != nil && useracct_itemCache.row.Key == key {
		row := *useracct_itemCache.row
		useracct_itemCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	useracct_itemCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	useracct_itemCache.lock.Lock()
	defer useracct_itemCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if useracct_itemCache.row != nil && useracct_itemCache.row.Key == key {
		return mustJSON(key), *useracct_itemCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushUseracctItemCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tUseracctItem"))
	if bucket == nil {
		panic("missing bucket tUseracctItem")
	}
	keyBytes := mustJSON(key)
	var row UseracctItem
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	useracct_itemCache.row = &row
	useracct_itemCache.isDirty = false

	return keyBytes, row
}

// putUseracctItem updates the cache and marks it as dirty (write-back).
func putUseracctItem(tx *bolt.Tx, key UseracctItemKey, row UseracctItem) {
	useracct_itemCache.lock.Lock()
	defer useracct_itemCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if useracct_itemCache.row != nil && useracct_itemCache.row.Key != key {
		flushUseracctItemCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	useracct_itemCache.row = &row
	useracct_itemCache.isDirty = true
}

func getUseracctItemPar(key UseracctItemKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.ui_u_id)
}

func putUseracctItemPar(key UseracctItemKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.ui_u_id)
}

type UseracctFeedbackKey struct {
	uf_u_id uint64
	uf_i_id uint64
	uf_i_u_id uint64
	uf_from_id uint64
}

type UseracctFeedback struct {
	Key UseracctFeedbackKey
	uf_rating uint64
}

// flushUseracctFeedbackCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushUseracctFeedbackCacheUnlocked(tx *bolt.Tx) {
	if useracct_feedbackCache.row != nil && useracct_feedbackCache.isDirty {
		bucket := tx.Bucket([]byte("tUseracctFeedback"))
		if bucket == nil {
			panic("missing bucket tUseracctFeedback")
		}
		var key UseracctFeedbackKey = useracct_feedbackCache.row.Key
		var row UseracctFeedback = *useracct_feedbackCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	useracct_feedbackCache.row = nil
	useracct_feedbackCache.isDirty = false
}

// flushUseracctFeedbackCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushUseracctFeedbackCache(tx *bolt.Tx) {
	useracct_feedbackCache.lock.Lock()
	defer useracct_feedbackCache.lock.Unlock()
	flushUseracctFeedbackCacheUnlocked(tx)
}

// getUseracctFeedback fetches data, utilizing the cache in a highly concurrent and safe manner.
func getUseracctFeedback(tx *bolt.Tx, key UseracctFeedbackKey) ([]byte, UseracctFeedback) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	useracct_feedbackCache.lock.RLock()
	if useracct_feedbackCache.row != nil && useracct_feedbackCache.row.Key == key {
		row := *useracct_feedbackCache.row
		useracct_feedbackCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	useracct_feedbackCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	useracct_feedbackCache.lock.Lock()
	defer useracct_feedbackCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if useracct_feedbackCache.row != nil && useracct_feedbackCache.row.Key == key {
		return mustJSON(key), *useracct_feedbackCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushUseracctFeedbackCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tUseracctFeedback"))
	if bucket == nil {
		panic("missing bucket tUseracctFeedback")
	}
	keyBytes := mustJSON(key)
	var row UseracctFeedback
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	useracct_feedbackCache.row = &row
	useracct_feedbackCache.isDirty = false

	return keyBytes, row
}

// putUseracctFeedback updates the cache and marks it as dirty (write-back).
func putUseracctFeedback(tx *bolt.Tx, key UseracctFeedbackKey, row UseracctFeedback) {
	useracct_feedbackCache.lock.Lock()
	defer useracct_feedbackCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if useracct_feedbackCache.row != nil && useracct_feedbackCache.row.Key != key {
		flushUseracctFeedbackCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	useracct_feedbackCache.row = &row
	useracct_feedbackCache.isDirty = true
}

func getUseracctFeedbackPar(key UseracctFeedbackKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.uf_u_id)
}

func putUseracctFeedbackPar(key UseracctFeedbackKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.uf_u_id)
}

type UseracctWatchKey struct {
	uw_u_id uint64
	uw_i_id uint64
	uw_i_u_id uint64
}

type UseracctWatch struct {
	Key UseracctWatchKey
}

// flushUseracctWatchCacheUnlocked contains the core logic for flushing.
// It is unexported and "unsafe" because it assumes the caller has already acquired the necessary lock.
func flushUseracctWatchCacheUnlocked(tx *bolt.Tx) {
	if useracct_watchCache.row != nil && useracct_watchCache.isDirty {
		bucket := tx.Bucket([]byte("tUseracctWatch"))
		if bucket == nil {
			panic("missing bucket tUseracctWatch")
		}
		var key UseracctWatchKey = useracct_watchCache.row.Key
		var row UseracctWatch = *useracct_watchCache.row
		putData(bucket, mustJSON(key), mustJSON(row))
	}
	// Clear the cache after flushing.
	useracct_watchCache.row = nil
	useracct_watchCache.isDirty = false
}

// flushUseracctWatchCache is the public, thread-safe API for flushing the cache.
// It acquires the lock before calling the core logic.
func flushUseracctWatchCache(tx *bolt.Tx) {
	useracct_watchCache.lock.Lock()
	defer useracct_watchCache.lock.Unlock()
	flushUseracctWatchCacheUnlocked(tx)
}

// getUseracctWatch fetches data, utilizing the cache in a highly concurrent and safe manner.
func getUseracctWatch(tx *bolt.Tx, key UseracctWatchKey) ([]byte, UseracctWatch) {
	// 1. Fast path (Read-only): Use a read lock for concurrent cache checks.
	useracct_watchCache.lock.RLock()
	if useracct_watchCache.row != nil && useracct_watchCache.row.Key == key {
		row := *useracct_watchCache.row
		useracct_watchCache.lock.RUnlock()
		return mustJSON(key), row // Cache Hit
	}
	useracct_watchCache.lock.RUnlock()

	// 2. Slow path (Write path): Acquire an exclusive lock.
	useracct_watchCache.lock.Lock()
	defer useracct_watchCache.lock.Unlock()

	// 3. Double-check in case another goroutine populated the cache while we waited for the lock.
	if useracct_watchCache.row != nil && useracct_watchCache.row.Key == key {
		return mustJSON(key), *useracct_watchCache.row // Cache Hit
	}

	// 4. Cache Miss: Call the "unlocked" helper since we already hold the lock.
	flushUseracctWatchCacheUnlocked(tx)

	// 5. Proceed with DB lookup.
	bucket := tx.Bucket([]byte("tUseracctWatch"))
	if bucket == nil {
		panic("missing bucket tUseracctWatch")
	}
	keyBytes := mustJSON(key)
	var row UseracctWatch
	if data := bucket.Get(keyBytes); data != nil {
		if err := json.Unmarshal(data, &row); err != nil {
			panic(err)
		}
	}
	
	// 6. Update cache with the newly fetched, clean row.
	useracct_watchCache.row = &row
	useracct_watchCache.isDirty = false

	return keyBytes, row
}

// putUseracctWatch updates the cache and marks it as dirty (write-back).
func putUseracctWatch(tx *bolt.Tx, key UseracctWatchKey, row UseracctWatch) {
	useracct_watchCache.lock.Lock()
	defer useracct_watchCache.lock.Unlock()

	// If the cache holds a different row, call the "unlocked" helper to flush it.
	if useracct_watchCache.row != nil && useracct_watchCache.row.Key != key {
		flushUseracctWatchCacheUnlocked(tx)
	}

	// Place the new row in the cache and mark it as dirty.
	row.Key = key
	useracct_watchCache.row = &row
	useracct_watchCache.isDirty = true
}

func getUseracctWatchPar(key UseracctWatchKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.uw_u_id)
}

func putUseracctWatchPar(key UseracctWatchKey) uint64 {
	// Call partition function to determine shard
	return p_user(key.uw_u_id)
}

func p_user(x uint64) uint64 {
	goto BB0

BB0:
	return x
}

func p_region(x uint64) uint64 {
	goto BB1

BB1:
	return x
}

func Scheds() {
	// Initialize scheduler maps
	LockSchedulers = make(map[string]*Scheduler)
	AccessSchedulers = make(map[string]*Scheduler)
	AccessDSchedulers = make(map[string]*Scheduler)

	// Create one lock scheduler for each table
	LockSchedulers["Useracct"] = NewScheduler()
	LockSchedulers["Region"] = NewScheduler()
	LockSchedulers["Item"] = NewScheduler()
	LockSchedulers["ItemBid"] = NewScheduler()
	LockSchedulers["ItemMaxBid"] = NewScheduler()
	LockSchedulers["ItemComment"] = NewScheduler()
	LockSchedulers["UseracctItem"] = NewScheduler()
	LockSchedulers["UseracctFeedback"] = NewScheduler()
	LockSchedulers["UseracctWatch"] = NewScheduler()

}

func Chains(db *bolt.DB) []*Chain {
	return []*Chain{
		CloseauctionsChainImpl(db),
		CloseauctionsnobidsChainImpl(db),
		GetitemChainImpl(db),
		GetuserinfoChainImpl(db),
		NewbidChainImpl(db),
		NewcommentChainImpl(db),
		NewcommentresponseChainImpl(db),
		NewfeedbackChainImpl(db),
		NewitemChainImpl(db),
		NewpurchaseChainImpl(db),
		UpdateitemChainImpl(db),
	}
}

