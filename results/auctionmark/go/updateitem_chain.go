// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type UpdateitemChain struct {
	db   *bolt.DB
	hops []*Hop
}

// UpdateitemHop0 lowers hop 0 containing 1 basic block(s).
func UpdateitemHop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var item_id uint64
	var seller_id uint64
	var new_name string
	var item_i_id uint64
	var item_i_u_id uint64
	var item_i_name string
	var item_i_initial_price float32
	var item_i_current_price float32
	var item_i_num_bids uint64
	var item_i_end_date uint64
	var item_i_start_date uint64
	var item_i_status uint64
	var item_i_num_comments uint64
	var item_i_c_id uint64

	var keyBytes1 []byte
	var row1 Item
	var keyBytes2 []byte
	var row2 Item

	item_id = toUint64(params["item_id"])
	seller_id = toUint64(params["seller_id"])
	new_name = params["new_name"]

	goto BB19

BB19:
	// Combined table access: Item (10 operations)
	keyBytes1, row1 = getItem(tx, ItemKey{i_id: item_id, i_u_id: seller_id})
	rwSet = AddRWSet(rwSet, "Item", keyBytes1)
	item_i_c_id = row1.i_c_id
	item_i_current_price = row1.i_current_price
	item_i_end_date = row1.i_end_date
	item_i_id = row1.Key.i_id
	item_i_initial_price = row1.i_initial_price
	item_i_num_bids = row1.i_num_bids
	item_i_num_comments = row1.i_num_comments
	item_i_start_date = row1.i_start_date
	item_i_status = row1.i_status
	item_i_u_id = row1.Key.i_u_id
	item_i_name = new_name
	// Combined table access: Item (11 operations)
	keyBytes2, row2 = getItem(tx, ItemKey{i_id: item_id, i_u_id: seller_id})
	rwSet = AddRWSet(rwSet, "Item", keyBytes2)
	row2.i_c_id = item_i_c_id
	row2.i_current_price = item_i_current_price
	row2.i_end_date = item_i_end_date
	row2.Key.i_id = item_i_id
	row2.i_initial_price = item_i_initial_price
	row2.i_name = item_i_name
	row2.i_num_bids = item_i_num_bids
	row2.i_num_comments = item_i_num_comments
	row2.i_start_date = item_i_start_date
	row2.i_status = item_i_status
	row2.Key.i_u_id = item_i_u_id
	putItem(tx, ItemKey{i_id: item_id, i_u_id: seller_id}, row2)
	// return - no action
	// Flush caches for tables written in this hop
	flushItemCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// UpdateitemHop0Par calculates the partition for hop 0 without database access.
func UpdateitemHop0Par(params map[string]string) uint64 {
	var item_id uint64
	var seller_id uint64
	var new_name string
	var item_i_id uint64
	var item_i_u_id uint64
	var item_i_name string
	var item_i_initial_price float32
	var item_i_current_price float32
	var item_i_num_bids uint64
	var item_i_end_date uint64
	var item_i_start_date uint64
	var item_i_status uint64
	var item_i_num_comments uint64
	var item_i_c_id uint64

	var keyBytes1 []byte
	var row1 Item
	var keyBytes2 []byte
	var row2 Item

	item_id = toUint64(params["item_id"])
	seller_id = toUint64(params["seller_id"])
	new_name = params["new_name"]

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB19

BB19:
	// First table access (optimized group) - calculate partition: Item
	if true { return getItemPar(ItemKey{i_id: item_id, i_u_id: seller_id}) }
	// Combined table access: Item (10 operations)
	keyBytes1, row1 = getItem(tx, ItemKey{i_id: item_id, i_u_id: seller_id})
	rwSet = AddRWSet(rwSet, "Item", keyBytes1)
	item_i_c_id = row1.i_c_id
	item_i_current_price = row1.i_current_price
	item_i_end_date = row1.i_end_date
	item_i_id = row1.Key.i_id
	item_i_initial_price = row1.i_initial_price
	item_i_num_bids = row1.i_num_bids
	item_i_num_comments = row1.i_num_comments
	item_i_start_date = row1.i_start_date
	item_i_status = row1.i_status
	item_i_u_id = row1.Key.i_u_id
	item_i_name = new_name
	// First table access (optimized group) - calculate partition: Item
	if true { return putItemPar(ItemKey{i_id: item_id, i_u_id: seller_id}) }
	// Combined table access: Item (11 operations)
	keyBytes2, row2 = getItem(tx, ItemKey{i_id: item_id, i_u_id: seller_id})
	rwSet = AddRWSet(rwSet, "Item", keyBytes2)
	row2.i_c_id = item_i_c_id
	row2.i_current_price = item_i_current_price
	row2.i_end_date = item_i_end_date
	row2.Key.i_id = item_i_id
	row2.i_initial_price = item_i_initial_price
	row2.i_name = item_i_name
	row2.i_num_bids = item_i_num_bids
	row2.i_num_comments = item_i_num_comments
	row2.i_start_date = item_i_start_date
	row2.i_status = item_i_status
	row2.Key.i_u_id = item_i_u_id
	putItem(tx, ItemKey{i_id: item_id, i_u_id: seller_id}, row2)
	return 0
}

func UpdateitemNextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(UpdateitemHop0Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func UpdateitemChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 1)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    UpdateitemHop0,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: UpdateitemNextReq}
}

// legacy: use json package to avoid unused import error
func UpdateitemToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

