// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type UpdateitemtitleChain struct {
	db   *bolt.DB
	hops []*Hop
}

// UpdateitemtitleHop0 lowers hop 0 containing 1 basic block(s).
func UpdateitemtitleHop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var iid uint64
	var title string
	var i_i_id uint64
	var i_title string

	var keyBytes1 []byte
	var row1 Item
	var keyBytes2 []byte
	var row2 Item

	iid = toUint64(params["iid"])
	title = params["title"]

	goto BB10

BB10:
	// TableGet: Item
	keyBytes1, row1 = getItem(tx, ItemKey{i_id: iid})
	rwSet = AddRWSet(rwSet, "Item", keyBytes1)
	i_i_id = row1.Key.i_id
	i_title = title
	// Combined table access: Item (2 operations)
	keyBytes2, row2 = getItem(tx, ItemKey{i_id: iid})
	rwSet = AddRWSet(rwSet, "Item", keyBytes2)
	row2.Key.i_id = i_i_id
	row2.title = i_title
	putItem(tx, ItemKey{i_id: iid}, row2)
	// return - no action
	// Flush caches for tables written in this hop
	flushItemCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// UpdateitemtitleHop0Par calculates the partition for hop 0 without database access.
func UpdateitemtitleHop0Par(params map[string]string) uint64 {
	var iid uint64
	var title string
	var i_i_id uint64
	var i_title string

	var keyBytes1 []byte
	var row1 Item
	var keyBytes2 []byte
	var row2 Item

	iid = toUint64(params["iid"])
	title = params["title"]

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB10

BB10:
	// First table access - calculate partition: Item
	if true { return getItemPar(ItemKey{i_id: iid}) }
	// TableGet: Item
	keyBytes1, row1 = getItem(tx, ItemKey{i_id: iid})
	rwSet = AddRWSet(rwSet, "Item", keyBytes1)
	i_i_id = row1.Key.i_id
	i_title = title
	// First table access (optimized group) - calculate partition: Item
	if true { return putItemPar(ItemKey{i_id: iid}) }
	// Combined table access: Item (2 operations)
	keyBytes2, row2 = getItem(tx, ItemKey{i_id: iid})
	rwSet = AddRWSet(rwSet, "Item", keyBytes2)
	row2.Key.i_id = i_i_id
	row2.title = i_title
	putItem(tx, ItemKey{i_id: iid}, row2)
	return 0
}

func UpdateitemtitleNextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(UpdateitemtitleHop0Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func UpdateitemtitleChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 1)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    UpdateitemtitleHop0,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: UpdateitemtitleNextReq}
}

// legacy: use json package to avoid unused import error
func UpdateitemtitleToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

