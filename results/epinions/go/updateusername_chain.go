// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type UpdateusernameChain struct {
	db   *bolt.DB
	hops []*Hop
}

// UpdateusernameHop0 lowers hop 0 containing 1 basic block(s).
func UpdateusernameHop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var uid uint64
	var name string
	var u_u_id uint64
	var u_name string

	var keyBytes1 []byte
	var row1 Useracct
	var keyBytes2 []byte
	var row2 Useracct

	uid = toUint64(params["uid"])
	name = params["name"]

	goto BB13

BB13:
	// TableGet: Useracct
	keyBytes1, row1 = getUseracct(tx, UseracctKey{u_id: uid})
	rwSet = AddRWSet(rwSet, "Useracct", keyBytes1)
	u_u_id = row1.Key.u_id
	u_name = name
	// Combined table access: Useracct (2 operations)
	keyBytes2, row2 = getUseracct(tx, UseracctKey{u_id: uid})
	rwSet = AddRWSet(rwSet, "Useracct", keyBytes2)
	row2.name = u_name
	row2.Key.u_id = u_u_id
	putUseracct(tx, UseracctKey{u_id: uid}, row2)
	// return - no action
	// Flush caches for tables written in this hop
	flushUseracctCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// UpdateusernameHop0Par calculates the partition for hop 0 without database access.
func UpdateusernameHop0Par(params map[string]string) uint64 {
	var uid uint64
	var name string
	var u_u_id uint64
	var u_name string

	var keyBytes1 []byte
	var row1 Useracct
	var keyBytes2 []byte
	var row2 Useracct

	uid = toUint64(params["uid"])
	name = params["name"]

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB13

BB13:
	// First table access - calculate partition: Useracct
	if true { return getUseracctPar(UseracctKey{u_id: uid}) }
	// TableGet: Useracct
	keyBytes1, row1 = getUseracct(tx, UseracctKey{u_id: uid})
	rwSet = AddRWSet(rwSet, "Useracct", keyBytes1)
	u_u_id = row1.Key.u_id
	u_name = name
	// First table access (optimized group) - calculate partition: Useracct
	if true { return putUseracctPar(UseracctKey{u_id: uid}) }
	// Combined table access: Useracct (2 operations)
	keyBytes2, row2 = getUseracct(tx, UseracctKey{u_id: uid})
	rwSet = AddRWSet(rwSet, "Useracct", keyBytes2)
	row2.name = u_name
	row2.Key.u_id = u_u_id
	putUseracct(tx, UseracctKey{u_id: uid}, row2)
	return 0
}

func UpdateusernameNextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(UpdateusernameHop0Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func UpdateusernameChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 1)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    UpdateusernameHop0,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: UpdateusernameNextReq}
}

// legacy: use json package to avoid unused import error
func UpdateusernameToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

