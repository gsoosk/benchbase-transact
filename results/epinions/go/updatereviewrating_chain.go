// Code generated by FMitF Go code generator. DO NOT EDIT.

package chainManager

import (
	"encoding/json"
	"github.com/boltdb/bolt"
	"ShardDB/proto"
	"ShardDB/util"
)

type UpdatereviewratingChain struct {
	db   *bolt.DB
	hops []*Hop
}

// UpdatereviewratingHop0 lowers hop 0 containing 1 basic block(s).
func UpdatereviewratingHop0(tx *bolt.Tx, in *proto.TrxReq) (*proto.TrxRes, error) {
	var rwSet []*proto.RWSet
	params := in.Params

	var iid uint64
	var uid uint64
	var rating uint64
	var r_u_id uint64
	var r_i_id uint64
	var r_rating uint64
	var r_creation_date uint64

	var keyBytes1 []byte
	var row1 Review
	var keyBytes2 []byte
	var row2 Review

	iid = toUint64(params["iid"])
	uid = toUint64(params["uid"])
	rating = toUint64(params["rating"])

	goto BB11

BB11:
	// Combined table access: Review (3 operations)
	keyBytes1, row1 = getReview(tx, ReviewKey{u_id: uid, i_id: iid})
	rwSet = AddRWSet(rwSet, "Review", keyBytes1)
	r_creation_date = row1.creation_date
	r_i_id = row1.Key.i_id
	r_u_id = row1.Key.u_id
	r_rating = rating
	// Combined table access: Review (4 operations)
	keyBytes2, row2 = getReview(tx, ReviewKey{u_id: uid, i_id: iid})
	rwSet = AddRWSet(rwSet, "Review", keyBytes2)
	row2.creation_date = r_creation_date
	row2.Key.i_id = r_i_id
	row2.rating = r_rating
	row2.Key.u_id = r_u_id
	putReview(tx, ReviewKey{u_id: uid, i_id: iid}, row2)
	// return - no action
	// Flush caches for tables written in this hop
	flushReviewCache(tx)
	return &proto.TrxRes{
		Status: proto.Status_Success,
		Info:   in.Info,
		RWSets: rwSet,
	}, nil
}

// UpdatereviewratingHop0Par calculates the partition for hop 0 without database access.
func UpdatereviewratingHop0Par(params map[string]string) uint64 {
	var iid uint64
	var uid uint64
	var rating uint64
	var r_u_id uint64
	var r_i_id uint64
	var r_rating uint64
	var r_creation_date uint64

	var keyBytes1 []byte
	var row1 Review
	var keyBytes2 []byte
	var row2 Review

	iid = toUint64(params["iid"])
	uid = toUint64(params["uid"])
	rating = toUint64(params["rating"])

	var tx *bolt.Tx = nil // Fake tx for unreachable code
	var rwSet []*proto.RWSet // Fake rwSet for unreachable code
	_ = tx // Suppress unused variable warning
	_ = rwSet // Suppress unused variable warning

	goto BB11

BB11:
	// First table access (optimized group) - calculate partition: Review
	if true { return getReviewPar(ReviewKey{u_id: uid, i_id: iid}) }
	// Combined table access: Review (3 operations)
	keyBytes1, row1 = getReview(tx, ReviewKey{u_id: uid, i_id: iid})
	rwSet = AddRWSet(rwSet, "Review", keyBytes1)
	r_creation_date = row1.creation_date
	r_i_id = row1.Key.i_id
	r_u_id = row1.Key.u_id
	r_rating = rating
	// First table access (optimized group) - calculate partition: Review
	if true { return putReviewPar(ReviewKey{u_id: uid, i_id: iid}) }
	// Combined table access: Review (4 operations)
	keyBytes2, row2 = getReview(tx, ReviewKey{u_id: uid, i_id: iid})
	rwSet = AddRWSet(rwSet, "Review", keyBytes2)
	row2.creation_date = r_creation_date
	row2.Key.i_id = r_i_id
	row2.rating = r_rating
	row2.Key.u_id = r_u_id
	putReview(tx, ReviewKey{u_id: uid, i_id: iid}, row2)
	return 0
}

func UpdatereviewratingNextReq(in *proto.TrxRes, params map[string]string, shards int) (*proto.TrxReq, map[string]string, int) {
	hopId := in.Info.Hopid
	req := &proto.TrxReq{Info: in.Info}
	nextShard := 0

	switch hopId {
	case 0:
		// Deep copy params to avoid modification
		paramsCopy := make(map[string]string)
		for k, v := range params {
			paramsCopy[k] = v
		}
		// Calculate partition for next hop using partition function
		nextShard = int(UpdatereviewratingHop0Par(paramsCopy))
	default:
		return nil, params, 0
	}

	req.Params = params
	req.Info.Hopid += 1
	req.Dependency = in.Dependency
	return req, params, nextShard
}

func UpdatereviewratingChainImpl(db *bolt.DB) *Chain {
	// Create the list of hops
	hops := make([]*Hop, 1)

	hops[0] = &Hop{
		id:         0,
		hopType:    util.NormalHop,
		isReadOnly: false,
		process:    UpdatereviewratingHop0,
		conflicts:  map[int32]int32{},
	}

	// add field names
	return &Chain{db: db, hops: hops, NextReq: UpdatereviewratingNextReq}
}

// legacy: use json package to avoid unused import error
func UpdatereviewratingToJSON(v interface{}) ([]byte, error) {
	return json.Marshal(v)
}

